# Wiki 使用完整指南

> 给完全不懂技术的人。看完就能用。

## Wiki 是什么

Wiki 就是你的"第二大脑"。你把小说、剧本、项目文档扔进去，AI 自动帮你整理成能搜索、能画图、能追踪进度的知识库。

打个比方：你有一堆散乱的纸，Wiki 就是 AI 帮你把它们订成一本有目录、有索引、能翻查的书。

## 核心概念（可以不看）

- **.raw/** — 原材料。会话、参考资料和过程记录保留在真实位置，AI 不复制、不移动、不删除
- **wiki/** — 整理好的知识。AI 从 Raw 读取证据，将已确认结论写成结构化笔记
- **log.md** — 操作日志。AI 每次干了什么都记一笔

## 五个 Skill 分工

| Skill | 干啥 | 什么时候用 |
|-------|------|-----------|
| jc-everything-wiki | 建库或接管 | 刚开始 |
| jc-raw-wiki | 填充已确认内容 | 建好库之后 |
| jc-cha-wiki | 查进度/搜东西/画图 | 随时 |
| jc-jian-wiki | 体检，只查不改 | 定期 |
| jc-xiu-wiki | 修复确定性问题；给 Wiki 加新分类 | 巡检之后 / 想加分类时 |

记住一句话就够：**巡检官只挑错不动手，修正官负责把错改掉**。这两个搭配着用，查出来的问题才不会一直堆在报告里没人处理。

## 完整操作：七步走

下面每一步都是**完整指令**，照抄粘贴到对话框，改一下方括号里的内容就能用，不用自己拼词。

### 1. 建库

```
转Wiki：
[粘贴你的内容——可以是小说、剧本、对话记录，或者说"扫描当前项目目录"]
```

### 2. 填内容

```
请把本轮已确认的内容填充进 Wiki：
从 .raw/ 和已有证据读取新增内容，不复制会话原文；
按项目结构更新对应页面、补必要双链，并刷新 hot.md。
```

### 3. 查进度

```
请查看当前 Wiki 状态：
说明当前结构、最近确认事实、风险和下一步入口。
```

### 4. 搜东西

```
请查询 Wiki：
关键词：[你想查的角色名/设定名，比如"张三"或"世界观"]
```

### 5. 画关系图

```
请为当前角色生成关系图：
读取 wiki/ 里的 [[双链]]，生成 wiki/关系图.canvas。
```
生成后要装「Obsidian Canvas Viewer」插件才能看图。

### 6. 体检

```
请对当前知识库做全库巡检：
查换皮漏改、命名漂移、映射撞车、伏笔没收、双链断裂、孤儿笔记，
输出按优先级排序的体检单。
```
查出问题只会列清单，不会自己改。

### 7. 改错

```
请按最新巡检报告修正已确定的问题：
机械类问题（漏改/断链/命名不一致）直接改；
语义类问题（设定矛盾）先问我哪个对再改；
改完标记✅已修或⏭跳过，更新 log.md。
```
把第 6 步体检单上的问题一条条改掉。改完的问题会打勾，改不确定的会先问你哪个对，不会自己瞎改。

## 项目做大了：加分类

Wiki 刚建的时候分类往往是猜的——比如小说一开始只建了"角色、世界、剧情"，写到一半突然觉得伏笔太多该单独建个本子记，直接粘贴：

```
请给 Wiki 加一个新分类：
新分类：[分类名，比如"伏笔"]
用途：[一句话说明放什么内容，比如"追踪伏笔埋设和回收进度"]
```

`jc-xiu-wiki` 会建好新目录。如果这个新分类需要理解旧内容再迁移（比如把角色档案里的伏笔线索整理进新目录），会转交 `jc-raw-wiki`，因为这不是简单建文件夹。

体检的时候如果发现某个分类文件堆得特别多，巡检官也会主动提一句"要不要拆开"，看到这种提示答"好"或者直接说"帮我拆"就行。

## 速查表

| 指令 | 干什么 |
|------|--------|
| `转Wiki：[内容]` | 建知识库 |
| `填充 Wiki` | 填角色/大纲/日志 |
| `查看Wiki状态` | 看进度 |
| `查询[关键词]` | 搜东西 |
| `生成关系图` | 画关系网 |
| `巡检一下` | 健康检查，出问题清单 |
| `执行修正` | 把清单上的问题改掉 |
| `加个分类` | 给 wiki 建新的子目录 |

## 常见问题

### 小说太长会卡吗？

不会。建壳只建目录不读全文，填充时逐章读。

### 能自己改文件吗？

可以，但建议让 AI 通过指令改——它会自动补双链。

### Wiki 会和代码混在一起吗？

不会。wiki/ 和 .raw/ 是独立目录。

### 多个项目怎么切换？

每个项目有自己 wiki/，AI 自动找当前目录的。

### 怎么同步到 GitHub？

wiki/ 和 .raw/ 是普通文件夹，git add + commit + push 就行。

### 想重建？

不要删除原 Wiki 或 .raw/。想重建结构时，先保留现有资料并让 AI 接管；涉及迁移、合并或删除时，先明确确认范围。

### 巡检查出问题，为什么不直接帮我改了？

巡检官的角色就是"体检"，只负责查、不负责治。这是故意分开的：查错和改错混在一起容易查着查着手滑改错东西。查完说一声「执行修正」，交给修正官专门处理。

### 修正官会不会瞎改我的设定？

不会。能直接判断该改成什么的（比如漏改的错别字、断开的链接）它会直接改；但遇到两处设定说法不一样（比如档案说角色左脸有疤，正文写成右脸），它一定会先问你哪个对，不会自己挑一个改掉。
