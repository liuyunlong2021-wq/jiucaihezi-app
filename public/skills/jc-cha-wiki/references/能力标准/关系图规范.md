# 关系图规范

## 何时使用

只有多个实体或页面之间存在稳定、难以线性说明的关系时才生成 Canvas；单一事实查询不生成图。

## 输入

现有 Wiki 页面及其中的双链。缺失的双链和事实不由本能力补写。

## 输出标准

- 关系图是可再生衍生产物，节点和边必须能回到现有源页面。
- 不改源页面、不追加 `log.md`，不把关系图当成唯一事实源。
- 无法解析的链接不臆造边；在报告中说明其未解析状态。
