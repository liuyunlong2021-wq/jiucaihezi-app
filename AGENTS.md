# 韭菜盒子记忆工作台 - 开发哲学

> **最后更新**: 2026-08-05 · **分支**: `main` · **版本**: v2.1.9

## 0. 先审根因，再修症状

Bug 是症状，不是根因。修之前追溯完整链路：

1. 追溯全链路：什么改动 -> 什么坏了 -> 那个改动为什么存在？
2. 质疑每一环：它真的需要存在吗？
3. 去掉根因既能修 Bug 又能简化代码时，去掉它。
4. 只有根因确实必要，才修症状。

开发前先查知识库：

- [知识库首页](docs/wiki/CLAUDE.md)
- [产品架构](docs/wiki/架构/产品架构.md)
- [生存手册](docs/wiki/学习/AI编程生存手册.md)
- [热缓存](docs/wiki/hot.md)

## 1. 先复用再实现

- 先搜索本仓库已有的记忆工作台、Gateway 和平台适配模式。
- 再搜索 npm、GitHub 和官方文档中的成熟实现。
- 画布能力优先复用当前 `.canvas`、`.jccanvas`、`.jcscene` 链路。
- Apple Silicon 不是唯一目标；Windows、Intel Mac、Web 和 Mobile 的现有能力都不能被无意削弱。

动手前明确假设。有歧义就问，有更简单的方案就说明，不清楚就停下来。

## 2. 极简优先

- 不写没被要求的功能。
- 不抽象只使用一次的代码。
- 不增加猜测性的灵活性或配置。
- 能删除根因就不增加补丁层。
- 200 行能写成 50 行就重写。

## 3. 精准修改

- 只动任务必需的内容，不顺手改相邻代码或格式。
- 跟随现有风格，保留无关的用户改动。
- 只删除本任务确认无调用者、且不属于记忆工作台能力闭包的内容。
- `docs/wiki/归档/`、`docs/wiki/log.md`、`docs/wiki/开发/开发历史.md` 和 `CHANGELOG.md` 是历史证据，不因产品分离删除。

唯一产品边界：**记忆工作台现在拥有的全部功能都保留；记忆工作台现在没有的功能全部迁出。**

## 4. 目标驱动与 TDD

把任务转化为可验证目标：

- 加校验 -> 先为无效输入写测试，再让测试通过。
- 修 Bug -> 先写能复现根因的测试，再让测试通过。
- 重构或删除 -> 先锁定保留能力与禁入边界，再实施并跑回归。

多步骤任务先列出简短计划：

```text
1. [步骤] -> 验证：[检查方式]
2. [步骤] -> 验证：[检查方式]
3. [步骤] -> 验证：[检查方式]
```

没有执行的真实平台、生产发布或用户数据升级验证，不得写成已通过。
