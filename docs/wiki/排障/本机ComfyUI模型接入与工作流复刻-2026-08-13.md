# 本机 ComfyUI 模型接入与工作流复刻

> 日期：2026-08-13
> 状态：Z-Image Turbo 图片与 Grok 视频 30 秒工作流已在 macOS Desktop 开发版完成真实生成验收；本页是后续接入本机 ComfyUI 模型的实施与排障基线。

## 已确认的链路

韭菜盒子 Desktop 的创作面板可把本机模型作为 `local-comfy` 路由提交：`/prompt` 提交工作流，`/history/{prompt_id}` 等待完成，`/view` 读取结果；桌面端再下载结果并写入项目媒体。

本机实际 ComfyUI 地址为 `http://127.0.0.1:8000`，不是常见默认示例端口 `8188`。旧保存的 `8188` 值需要迁移为 `8000`，否则会在提交阶段失败。

Z-Image Turbo 在创作面板提供 `720p`、`1080p` 及 `16:9`、`9:16`、`4:3`、`3:4`、`1:1`。运行时把两项转换为 8 的倍数宽高后写入 ComfyUI 的 `EmptyLatentImage`。

## 接入规则

1. 先在 ComfyUI 手工运行目标工作流，确认模型文件、LoRA、采样器、正负提示词、尺寸和保存节点均可成功，再导出 API 工作流作为事实来源。
2. App 不按模型名重新拼一套“看似相同”的最小工作流。必须保留手工工作流里已经验证的节点、连线和参数，只替换用户真正需要填写的节点。
3. 为每个接入模型明确输入映射：提示词写入哪一个文本节点，比例/分辨率写入哪一个尺寸节点，参考图或视频写入哪一个输入节点。没有确认映射前不在 UI 暴露该模型。
4. 固定工作流中其余值原样保留，包括 LoRA 顺序与强度、负面提示词、采样器、scheduler、VAE、保存节点和文件名前缀；它们不是可随意删减的实现细节。
5. 后续 MiniMax H3 或其他模型按同一方法独立接入，不能复用 Z-Image 的节点 ID、模型文件名或工作流假设。

## Z-Image Turbo 已验证映射

当前本机工作流使用：

| 用户输入 | ComfyUI 节点 | 写入字段 |
| --- | --- | --- |
| 提示词 | `CR Text (47)` | `text`，再连到 `CLIPTextEncode (24)` |
| 分辨率与比例 | `EmptyLatentImage (21)` | `width`、`height` |

已保留的已验证节点：`UNETLoader (18)`、`LoraLoaderModelOnly (23 -> 14)`、`CLIPLoader (12)`、`VAELoader (11)`、正负 `CLIPTextEncode (24/16)`、`KSampler (22)`、`VAEDecode (17)`、`SaveImage (46)`。

`KSampler` 的已验证参数为 `steps=4`、`cfg=1`、`sampler_name=dpmpp_2s_ancestral`、`scheduler=simple`、`denoise=1`。随机种子使用 ComfyUI 同级的大整数范围；低范围旧种子会显著放大 4 步 Turbo 工作流的成片波动。

## 本轮错误与根因

| 现象 | 根因 | 已采用的处理 |
| --- | --- | --- |
| `/prompt` 请求失败 | 本机 ComfyUI 实际运行在 `8000`，App 保留了旧的 `8188` | 统一默认值并迁移旧保存地址 |
| 任务已完成但 App 报 `The string did not match the expected pattern.` | WebKit 前端 Blob/Base64 转换与后续保存链路不兼容 | 返回受限的本机 `/view` 地址，由 Desktop 后台下载并落盘 |
| 能生成但画面不按既有工作流表现 | 初版手写最小节点图，遗漏 `CR Text`、LoRA 和原负面提示词 | 改为复刻真实 API 工作流，只覆盖提示词和尺寸输入 |
| 修改后仍像旧版本 | 孤立的旧 Tauri 调试窗口失去 Vite `1420` 服务，无法热更新 | 重启 `pnpm tauri:dev`，并用运行中前端模块确认实际加载代码 |
| 同一提示词结果差异大、部分图发糊 | 采样参数并未缺失；随机种子不同，旧窗口还在使用低范围种子 | 以 ComfyUI 已成功种子做 A/B 对照，确认工作流正确后重启到新种子实现 |

提示词中出现不可见控制字符时，应先记录提交到 `/history` 的真实文本再判断；本轮它不是采样器或连线缺失的证据。不得仅凭成图肉眼判断节点是否接通。

## 最小验收顺序

1. `GET /system_stats`、`/object_info` 和所需模型列表均可读；记录真实端口与运行设备。
2. 手工 ComfyUI 与 App 使用同一提示词、同一尺寸、同一种子提交；比较 `/history` 中的完整节点 JSON。
3. 两份 JSON 除允许替换的输入节点与 `client_id` 外应一致；不一致时先修映射，不调采样参数。
4. 确认 `/view` 返回有效媒体，Desktop 项目目录已落盘，任务状态为成功。
5. 才允许开放随机种子；随后以多次随机生成观察模型正常波动，不把单张坏图误判为节点断链。

## 验证与边界

- 已通过：本机 ComfyUI `0.32.0`、MPS、Z-Image Turbo 的 API 提交、历史轮询、结果读取与用户真实 Desktop 成图验收；TypeScript、`localComfyRuntime`、`comfyUiRuntime` 和 URL 安全定向测试通过。
- 未通过/未做：MiniMax H3 及未单独登记的 ComfyUI 工作流尚未接入；Web、Mobile 不暴露本机 ComfyUI；未构建或发布三平台正式包。

## 韭菜盒子 ComfyUI 自定义节点首轮验收（2026-08-14）

本轮首次把韭菜盒子云端图片合同封装为独立的 ComfyUI custom node。用户已在本机 ComfyUI 实际生成成功。该节点是开发辅助能力，不接入韭菜盒子主 App 的构建、项目、画布或媒体保存链路。

### 已确认合同

- 插件目录为仓库 `comfyui-jiucaihezi/`，安装副本位于 `/Users/by3/Documents/ComfyUI/custom_nodes/comfyui-jiucaihezi/`；节点库只注册一个 `韭菜盒子 图片生成` 节点。
- 节点只暴露已确认字段：密码型 API Key、模型、提示词、分辨率、比例和最多 8 张 ComfyUI `IMAGE` 参考图。Key 必须在节点中显式填写，不读取韭菜盒子 Desktop 设置。
- GPT Image 2 可选 `gpt-image-2-1k`、低质量、中质量、VIP、官方。`1k` 档仅 `1k`；其余四档支持 `1k/2k/4k`；比例为 `1:1`、`2:3`、`3:2`、`4:5`、`5:4`、`4:3`、`3:4`、`16:9`、`9:16`、`21:9`。
- Gemini 可选 `gemini-3.1-flash-image-preview`、`gemini-3-pro-image-preview`，仅 `1k/2k`；前端选择 Gemini 时隐藏比例控件，避免暴露上游未确认参数。
- 节点提交 `POST /v1/videos`，带 `model`、`prompt`、`size`、`resolution`、`seconds=1` 和参考图；通过 `GET /v1/videos/{taskId}` 轮询，完成后返回 ComfyUI `IMAGE` 与原始响应文本。GPT 尺寸映射复用创作面板的完整比例表。

### 存储与复用规则

1. 结果只回到 ComfyUI 画布；连上 ComfyUI 内置 `Save Image` 后才保存到本机 `Documents/ComfyUI/output`。节点不向韭菜盒子主 App 写项目、画布或素材。
2. 后续接入创作面板其他模型，先以 `creationModelRegistry.ts` 和对应运维 Wiki 为唯一模型、尺寸、比例和参考图事实源；不能从第三方节点照抄字段或端点。
3. 一个同类能力优先合并为一个节点加模型选择器；模型之间参数不同，则用前端联动缩小下拉选项、隐藏不支持控件，并在 Python 提交前再次校验。
4. 每次安装后重启 ComfyUI，检查 `/object_info/<node>` 的真实 schema；通过自动测试后，仍需由用户完成一次实际生成，才能登记为可用。

### 验证范围

- 插件单元测试 `3/3` 通过：单节点模型/参考图 schema、模型分辨率合同、显式 Key 隔离。
- 本机 ComfyUI `http://127.0.0.1:8000` 已重启并注册该节点；`object_info` 确认输入为 `api_key`、`model`、`prompt`、`resolution`、`ratio` 与 8 个参考图。
- 用户已确认实际生成成功。本页不把这次图片节点验收外推为其他模型、其他 ComfyUI 节点或主 App 的生产验收。

## Grok 视频工作流首轮真机验收（2026-08-15）

用户已在 Desktop 创作面板选择 `Grok 视频 30 秒 · 本机 ComfyUI`，框选 4 张画布参考图并获得成功视频任务。该结果确认的是这一份已登记 API 工作流，不代表任意 ComfyUI JSON 可以直接执行。

### 已确认映射与安全边界

| App 输入 | ComfyUI 目标 |
| --- | --- |
| 提示词 | 节点 `16` |
| 参考图 1 至 7 | `LoadImage` 节点 `22、10、13、9、11、12、23` |
| 生成节点 | 节点 `7` |
| 结果读取 | `easy showAnything` 节点 `18`，连接生成节点输出 `["7", 3]` |

- 参考图按画布从上到下、同一行从左到右进入 1 至 7 号槽位；少于 7 张时只覆盖实际选择的槽位。
- 设置入口统一为 `API Key`。Desktop 使用本机安全存储，开发热更新期间只允许当前窗口会话缓存兜底；Key 不写入工作流文件、项目、Wiki、任务快照或日志。
- `/upload/image`、`/prompt` 和 `/history` 由 Desktop Rust HTTP 桥接访问。当前 ComfyUI 未启用 CORS，浏览器预检会返回 `403`，不能让 WebView 直接调用。
- 每张参考图使用独立上传文件名，避免 ComfyUI 的覆盖写入使多个槽位最终都指向最后一张图。
- 当前 ComfyUI `0.32.0` 的 `ComflyVideoAdapter.save_to()` 不接受 `crf` 参数。工作流不再经过 `SaveVideo`，而是读取生成节点返回的视频地址，再交给现有媒体任务链路处理。

### 本轮错误、根因与处理

| 现象 | 根因 | 处理 |
| --- | --- | --- |
| 已保存 Key，仍提示“请配置 API Key” | Vite/Tauri 开发进程和媒体任务执行器仍保留旧运行函数 | 修改 Runtime 后完整重启开发 App；不要用“新界面、旧执行函数”的窗口验收 |
| `Load failed` | WebView 直连 ComfyUI 触发 CORS，`OPTIONS` 为 `403` | 统一走 Desktop Rust HTTP 桥 |
| `missing field mime_type` | Tauri 调用使用了前端驼峰字段，Rust 命令要求 snake_case | 上传参数改为 `mime_type`、`data_base64` |
| 多张图可能全部变成最后一张 | 所有上传共用同一文件名且允许覆盖 | 每个槽位生成唯一文件名 |
| 上游生成完成后仍失败，日志含 `unexpected keyword argument 'crf'` | `SaveVideo` 与本机 ComfyUI/节点版本不兼容 | 删除该结果保存节点，直接读取生成节点返回的视频地址 |
| 第一次点“在创作面板中调整”右侧为空，关闭再开才显示 | 外层先挂载空宿主，`nextTick` 不会等待异步 `CreationPanel` 模块加载完成 | 统一打开入口先等待模块加载，再挂载右侧面板和投递媒体计划 |

### 验证与剩余边界

- 用户真实验收：2026-08-15 15:49，4 张画布参考图、提示词、模型参数和任务结果链路成功；不记录 Key、提示词、图片、视频地址和项目内容。
- 自动验证：本机 ComfyUI Runtime 测试 `5/5`、工作台专项 `54/54`、TypeScript 与 Rust 编译检查通过。
- 首次空白面板的加载顺序修复已通过专项测试和 TypeScript，仍待下一次 Desktop 点击复验。
- MiniMax H3、任意第三方节点、任意新工作流以及 Web/Mobile 本机执行均未由本轮证明；必须按 [[开发/本机ComfyUI工作流接入规范SDD]] 单独登记映射并真机验收。
