# 3D 编辑器导出与选中控件（2026-08-13）

> 状态：代码修复完成；图片预览与选中控件由用户确认，视频最终文件仍应按状态文本做 Desktop 实测。

## 已解决

### 导出诊断

- 截图、视频保存现在显示实际路径或失败原因。
- `dev_check_ffmpeg` 仅查找并执行系统已有的 `ffmpeg -version`；不内置、不下载 FFmpeg。
- 视频仍经 `dev_export_scene_video` 转为 H.264 MP4，写入 `.raw/jc-media/视频/`；同名自动加序号。

### 基础编辑

- 已提供 Delete/Backspace 删除、撤销/重做、复制/粘贴、名称、颜色、删除显示文字和右键菜单。
- 只修改并保存既有 `.jcscene`，不建立第二套格式或编辑器。

### 选中箭头遮挡

- 空白点击原先只尝试命中物体，未命中时没有取消选择，TransformControls 持续附着。
- 统一 `clearSelection()`：左键点空白、Esc、截图、手动录像、动画录像开始前都会调用。

## 下次排查

1. 视频没出现在文件夹时，先看状态文本，再确认目录 `.raw/jc-media/视频/`。
2. 只有视频失败时，先检测系统 FFmpeg，不先归因于模型或文件树。
3. 截图有箭头时点空白或按 Esc；仍有问题则检查捕获入口是否先调用 `clearSelection()`。

## 验证边界

- `vue-tsc -b`、focused 测试构建和 `git diff --check` 通过。
- 用户已确认选中控件可隐藏；视频导出最终文件未在本轮重新做 Desktop 实测。

## 关联

- [[排障/创作画布本地图片视频预览空白-2026-08-13]]
- [[开发/通用记忆工作台本地三维科普动画与MP4导出SDD]]
- [[hot]]
- [[来源索引]]
