# iPhone 云项目下载覆盖本地无响应（2026-08-10）

> 状态：未解决，已暂停继续构建和真机试错。

## 现象与预期

- 预期：iPhone 项目中心点击云端项目，确认“下载并覆盖本地”后，已有同一项目则覆盖本地可同步文字；没有本地副本则在 App 管理目录创建项目并下载，随后切换到该项目。
- 实际：用户在真实 iPhone 13 Pro Max 上点击云端项目并确认下载后，没有可见结果，本地项目内容和同步状态均未更新。
- 2026-08-10 18:59 安装最后一版后记录 4 个本地项目的 `.raw/.sync/state.json` 修改时间；用户 19:07 前再次操作后复查，四个时间全部未变化：`iPhone验收未命名项目` 仍为 2026-07-27 22:10，`丹溪` 仍为 2026-07-28 21:56，`我的抖音 2` 仍为 2026-08-04 07:59，`我的抖音` 仍为 2026-08-04 08:35。
- 结论：本轮不能登记为修复成功；自动测试、IPA 构建、安装和启动都不能替代真实下载落盘验收。

## 本轮已做但未闭环的修改

- 项目中心删除了重复的第二个“下载并覆盖本地”按钮，只保留点击云端项目后的单一入口；“上传并覆盖云端”入口保留。
- 删除了此前试验性的手写 DOM 确认框、`pointerdown` 触摸补丁和临时诊断标记，`confirmAction` 恢复共用实现。
- 补充 iOS `MacIntel + maxTouchPoints > 1` 的移动端识别，避免 iPad/iPhone 桌面式 User-Agent 被当成普通 Desktop。
- `ProjectTextSync` 读取固定 `.raw/.sync/state.json` 时改为只列出 `.raw/.sync`，不再为了一个固定文件递归扫描整个项目；对应回归测试模拟“递归列表受 1000 条上限影响但固定目录仍可读取”。
- 最后一版 `2.1.17` 调试签名 IPA 成功构建、导出、安装并启动，Bundle ID 为 `com.jiucaihezi.mobile`，包内未发现开发服务器地址。

## 验证边界

- UI、移动端识别等前一阶段完整 focused 为 `1028/1028`，TypeScript 和 `git diff --check` 通过。
- 最后一次同步状态定向测试为 `7/7`，TypeScript 通过，iOS Release 构建、IPA 导出、真机安装和启动成功。
- 最终真实 iPhone 操作仍失败，状态文件时间未变化；因此上述自动验证只证明局部代码合同和构建链可用，不证明点击事件、确认回调、项目定位、网络下载或本地写入的真实链路已执行。
- 本轮未提交、未推送、未发布 TestFlight 或 App Store；工作区保留未提交修改，后续接手时不得把这些修改当成已验证修复。

## 下次接手入口

下一次只做一次可观测实验，不再继续盲改 UI：在 `openCloudProject` 的点击入口、确认返回、`localOwnerForCloud`、`projectTextSync.open/connect/downloadNow` 和最终错误出口记录同一操作 ID，并用真机日志确认最后到达的边界。只有定位到明确停点后才修改代码；验收必须同时满足“界面切换或错误可见”与“目标项目文件或 `.raw/.sync/state.json` 真实变化”。

## 关联

- [[开发/通用记忆对话独立App SDD]]
- [[架构/产品架构]]
- [[hot]]
- [[来源索引]]
