# 🔥 热缓存

> 当前开发阶段最需要被 AI 读的六份文档。

1. **[[开发/OpenCode官方信息流翻译SDD]]** — 已修复 plan `tools:false` 持久化污染 build 权限；callback id 已定位为 Vite HMR 后的旧 Channel。旧污染会话需新建或 fork，同会话文→武、正式包 Channel 及 Intel 等矩阵待验收。
2. **[[开发/创作模式双端统一SDD]]** — 创模式独立会话、直连工具循环、Desktop 文件适配与媒体任务均不启动 OpenCode；Desktop 只扫描 `~/.agents/skills`，内置 Skill 只从 `public/skills` 读取且不复制到用户目录。最新权限规则：每个任务可选“拒绝 / 允许 / 始终允许”；始终允许后模型可在本次任务读、查、写、改项目外绝对路径（包括 `/tmp`）并运行终端，任务结束自动失效。长视频已移除 12 轮、4096 tokens、120 秒三处产品硬截断；首次正文流断会自动续写一次。仍需真实视频工作流验收。
3. **[[开发/Web云端项目Wiki媒体同步与APP升级SDD]]** — Web 项目适配已完成：项目树、画布、上传/导入导出、站内预览和创作媒体均使用当前浏览器 IndexedDB + OPFS；创作任务冻结提交时项目，媒体落 `jc-media`。无 D1/R2/服务器媒体存储或跨浏览器同步。
4. **[[开发/画布开发与排障]]** — 创作面板画布架构、保存恢复、工具、性能与已知问题的唯一入口；2026-07-16 项目文件树改为虚拟行渲染，视频缩略图由桌面后台生成并缓存，避免首次打开大量视频卡住界面。定向任务仍以 `owner:path` 队列和 scoped gate 保护 `read -> modify -> write`。
5. **[[开发/全仓测试失败审计-2026-07-13]]** — 测试债务已清理：Node 747/747、Rust 371/371；剩余是文→武、Intel/Ollama/交互人工矩阵。
6. **[[架构/对照表]]** — 韭菜盒子 ↔ OpenCode 文件映射，发现 Bug 的入口。

---

> 上次刷新: 2026-07-16（创作模式已修复文字路径误作附件、12 轮工具上限、4096 输出上限和 SSE 120 秒总超时；仍待真实视频工作流验收）
