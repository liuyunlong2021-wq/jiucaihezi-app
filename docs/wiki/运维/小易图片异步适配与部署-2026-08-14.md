# 小易图片异步适配与部署

> 实施日期：2026-08-14
> 状态：生产适配器已部署且健康；1K 与三参考图端到端真实成功；小易账号池存在间歇波动；官方稳定渠道待部署验收。

## 目标与边界

不修改 NewAPI 官方源码。NewAPI 继续负责用户鉴权、渠道选择、计费和任务持久化；独立 `xiaoyi-image-adapter` 只负责把 NewAPI 现有 `/v1/videos` 异步任务合同转换为小易图片异步接口。

```text
App -> NewAPI /v1/videos
    -> xiaoyi-image-adapter
    -> 小易 /v1/images/generations/async 或 /v1/images/edits/async
    <- 小易 /v1/images/tasks/{task_id}
    <- NewAPI /v1/videos/{task_id}
```

## 模型与渠道合同

| App / NewAPI 模型 | 小易上游模型 | 分辨率 | 基础价 |
|---|---|---|---|
| `gpt-image-2-1k` | `gpt-image-2` | 1K | 0.08/张 |
| `gpt-image-2-低质量` | `gpt-image-2` | 1K-4K | 0.1/张 |
| `gpt-image-2-中质量` | `gpt-image-2` | 1K-4K | 0.15/张 |
| `gpt-image-2-vip` | `gpt-image-2-vip` | 1K-4K | 0.2/张 |
| `gpt-image-2-官方` | `gpt-image-2` | 1K-4K | 0.25/张 |
| `gemini-3-pro-image-preview` | 同名 | 1K/2K | 0.2/张 |
| `gemini-3.1-flash-image-preview` | 同名 | 1K/2K | 0.1/张 |

前三个 GPT 别名和 `gpt-image-2-官方` 由适配器映射到小易真实模型 `gpt-image-2`；VIP 和两个 Gemini 使用上游真实模型名。历史 `gpt-image-2` 计划统一兼容到支持 1K-4K 的低质量档，避免旧 2K/4K 任务被错误送入仅支持 1K 的渠道。新工作台图片计划未指定分辨率或选择 1K 时使用 1K 档，选择 2K/4K 时使用低质量档。

## 本次根因与修复

1. NewAPI 的视频任务合同在未传 `seconds` 时默认按 4 秒处理，可能导致图片价格乘 4。App 的小易图片请求现固定提交 `seconds=1`，仍按单张任务计费。
2. NewAPI 会在转发前应用模型映射。适配器除六个公开模型外，也接受映射后的隐藏 canonical `gpt-image-2`，但 `/v1/models` 不公开该名称。
3. 小易轮询的一次网络错误、429 或 5xx 不能代表图片任务失败。适配器将这些瞬时错误保持为 `processing`，避免 NewAPI 永久失败并退款。
4. NewAPI 的 OpenAI 类型任务不会保存提交时的渠道 Key，而小易要求提交和查询使用同一 Key。因此每个图片渠道只能配置一把 Key，任务运行期间不得换 Key。
5. Gemini 上游当前合同只确认 1K/2K，App 已移除无效的 4K 和比例控件，禁止静默降级。
6. Web 的 GPT Base64 大图已允许在可信创作结果落盘路径突破默认 12 MB Data URL 限制；普通不可信 Data URL 仍受原限制。
7. 参考图入口限制为最多 8 张、单张 20 MB、总计 64 MB，且只接受 `image/*`。

## NewAPI 配置

每个质量渠道保持独立，不把多个价格档合并进同一渠道：

- 渠道类型：`OpenAI`
- Base URL：`http://xiaoyi-image-adapter:8793`
- 每渠道只保留该渠道明确支持的模型名
- 每渠道只配置一把小易 Key
- 关闭上游模型自动同步和自动添加
- 现有前三个 GPT 模型映射可以保留；适配器已兼容映射后的 `gpt-image-2`

## 生产部署

服务器沿用 `/opt/jiucai-repo` 稀疏仓库和 `/opt/<adapter>` 独立运行目录：

```bash
set -euo pipefail

cd /opt/jiucai-repo
git fetch origin main
git checkout main
git pull --ff-only origin main
git sparse-checkout add xiaoyi-image-adapter

mkdir -p /opt/xiaoyi-image-adapter
rsync -a --delete \
  /opt/jiucai-repo/xiaoyi-image-adapter/ \
  /opt/xiaoyi-image-adapter/

cd /opt/xiaoyi-image-adapter
docker compose config
docker compose up -d --force-recreate --build
docker compose ps
docker compose logs --tail=100
```

容器内健康检查：

```bash
cd /opt/xiaoyi-image-adapter
docker compose exec -T xiaoyi-image-adapter \
  python -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:8793/health').read().decode())"
docker compose exec -T xiaoyi-image-adapter \
  python -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:8793/v1/models').read().decode())"
```

## 验证结果与未验证边界

- 适配器单元测试：8/8 通过。
- 前端 focused：1037/1037 通过。
- Rust：396 通过、1 ignored。
- TypeScript、Docker Compose 配置、Web 正式构建和 Web 产物审计通过。
- 生产 Docker 镜像构建、容器启动和容器内 `/health` 已由用户执行成功，六个公开模型均出现在健康响应中。
- NewAPI 渠道 88 的 `gpt-image-2-1k -> gpt-image-2` 已真实生成成功，证明 App、NewAPI、适配器、小易异步提交与轮询主链可用。
- 渠道 91 的低质量任务真实返回 `fetch failed`；渠道 92 和 115 的 2K 任务真实返回 `No available 2K image accounts`。
- 绕过 App、NewAPI 和适配器后，直接向小易提交 `gpt-image-2-vip` 三参考图 2K 编辑任务仍返回 `No available 2K image accounts`，已排除本方模型映射和参考图上传格式。
- 此前长时间等待的三参考图任务后来成功，并由 App 落盘，确认端到端主链可用；先前账号池错误属于间歇性上游可用性问题。
- `gpt-image-2-官方 -> gpt-image-2` 已加入 App、可用性服务和适配器代码，但尚未重新部署及真实生成验收。
- 当前不能写成全部模型生产闭环通过；详细证据与错误判读见 [[排障/小易GPT图片多参考图与账号池失败-2026-08-14]]。
