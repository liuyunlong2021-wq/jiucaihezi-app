# 韭菜盒子媒体与 3D 能力迁出 TDD（已取消）

> 日期：2026-08-06
> 状态：**用户在实施前撤销迁出决定；未写入迁出 Red 测试，未执行迁出**
> 前置记录：[[开发/韭菜盒子媒体与3D能力迁出SDD]]

## 1. 取消结论

本 TDD 不再是待执行任务。不得继续创建以下测试或实现：

- 要求韭菜盒子发布物移除媒体、`.jccanvas`、`.jcscene` 或 Three.js 的门禁；
- 要求短视频工厂接收韭菜盒子媒体与 3D 能力的跨仓测试；
- 基于迁出假设删除模型、Gateway、任务历史、画布、3D、FFmpeg 或用户文件的实现。

## 2. 未执行事实

- 短视频工厂工作区未被本计划修改。
- 韭菜盒子没有迁出提交、源端删除提交或迁移发布。
- 没有真实平台、Provider 或用户数据迁移被声称通过。
- 现有媒体与 3D 回归测试继续作为能力保护门禁，而不是迁出阻碍。

## 3. 后续测试边界

未来若执行性能优化，应新建独立性能 TDD，只验证以下结果：

1. 普通启动、对话和 Wiki 操作不提前加载非活动 3D 场景或创作面板。
2. 3D 静止时可减少无意义刷新，拖动、播放、录制和导出时保持完整帧率与最终质量。
3. 媒体任务在 SQLite 就绪后可靠恢复，不丢历史、不重复提交、不覆盖真实任务。
4. 高性能设备的功能、画质、模型选择和正常并发不减少。

上述性能工作不得复用本文件名义重新启动迁出。

## 反向链接

- [[开发/韭菜盒子媒体与3D能力迁出SDD]]
- [[hot]]
