# 通用记忆对话工作台 SDD

> 日期：2026-07-27
> 状态：Web 与 Mac 已完成；第四阶段 4A、4B 已完成，4C 已完成 iPhone 真机编译、签名、安装，运行受 Tauri 官方 #14675 阻塞
> 首发目标：<https://jiucaihezi.studio>
> 依据：`AGENTS.md`、[[架构/产品架构]]、[[开发/Wiki四Skill产品化升级SDD]]、[[开发/文件系统/索引]]、[[开发/文件系统/文件树一期资源身份与文件安全SDD]]、[[开发/文件系统/Web云端项目Wiki媒体同步与APP升级SDD]]、[[开发/创模式MCP工具接入SDD]]、[[开发/韭菜盒子原生媒体编排能力SDD]]

## 1. 决断

先把现有韭菜盒子换成一个极简的通用记忆对话工作台：

```text
项目文件树 + 顶部对话选择器 + 中央内容区 + 隐藏设置
```

用户通过顶部对话选择器切换对话，通过文件树浏览资料：

```text
顶部对话选择器 -> 中央区显示完整聊天记录和输入框
Markdown/文本  -> 临时预览层显示文档阅读器
图片/视频/音频 -> 临时预览层显示预览或播放器
其他文件       -> 临时预览层显示安全打开/下载动作
```

文件树只负责资料导航和文件动作；顶部对话选择器只负责切换 Raw 对话。两者共用同一项目文件真源，不建立第二份对话数据库或索引。应用级项目入口统一列出本机项目和云端项目；进入工作台后不再增加第二套项目选择器、应用级模式导航、编辑区、创作面板或第三栏设置区。输入框上方只保留控制下一次发送的 `快速 | 记忆` 两段式开关。

产品能力仍然完整：登录、模型、Skill 仓库、MCP、附件、Wiki 读写和对话内图片/视频/音频生成都保留，只把设置收进按钮，把媒体生成收进对话。

## 2. 为什么这是正确边界

### 2.0 一个底座，多产品壳

通用记忆能力是后续产品共用的底座，不是把所有产品界面和运行时一起塞进一个安装包。仓库保持一个，交付物按产品独立：

```text
通用底座
├── 登录、Gateway 与云项目同步
├── ProjectFileService、Raw 与 Wiki
├── 对话、模型、Skill 与公共媒体能力
└── 跨端资源合同

独立产品壳
├── 记忆工作台 App
├── 韭菜盒子 Studio 主 App
├── 电商工作台 App
└── 漫剧工作台 App
```

每个产品壳只选择自己的前端入口、Bundle ID、Deep Link、更新通道和原生能力。共享底座不复制，未被目标入口引用的界面不进入前端产物；OpenCode sidecar 只属于 Studio，记忆、电商、漫剧和 Mobile 不得携带。当前已经发布的记忆 App 继续使用 `com.jiucaihezi.desktop`，避免现有用户的数据目录和升级链路断开；恢复主 Studio 时使用新的 `com.jiucaihezi.studio.desktop` 身份和独立发布通道。

### 2.1 对话导航与文件导航必须分离

每个对话本来就是 `.raw/对话记录/` 下的一个真实文件，但用户不应该为了继续聊天进入系统 Raw 目录寻找文件。顶部选择器只是 `.raw/对话记录/` 的 UI 投影，不是第二份存储。它读取对话标题、稳定 ID 和更新时间，完成创建、选择、重命名、删除和恢复；文件树不再承担对话切换。

### 2.2 所有文件都能临时预览，但不能都按聊天显示

“中央区”保持当前对话不被销毁；点击普通 Markdown、图片或其他文件时，打开统一的临时预览层。只有对话 Raw 才显示聊天气泡和输入框。

这样同时满足：

- 用户点任何可支持文件都能看到内容；
- 普通文档不会被误认为聊天记录；
- 不需要把系统 Raw 暴露成资料导航；
- 不需要重新引入独立编辑区或创作面板。

关闭预览层必须恢复原对话、滚动位置、输入草稿和附件状态。

### 2.3 资源判断只能有一个入口

现有 `openProjectResource()` 已经统一处理文档、媒体、画布和二进制。实现时只在这个共享路由增加 `conversation` 结果，不允许文件树、聊天区和移动端分别复制路径判断。

```text
ProjectFileTree
  -> openProjectResource(resource)
  -> conversation | document | media | binary
  -> CentralContentHost 切换对应视图
```

## 3. 产品边界

### 3.1 用户可以做什么

- 创建、导入或打开一个项目，并在下次启动恢复最近项目。
- 把当前项目上传为同账号云项目，或从云项目列表下载到当前设备后继续使用。
- 在文件树中浏览项目的 Raw、Wiki、媒体和其他文件。
- 新建任意名称的对话，点击任意对话继续聊天。
- 点击 Markdown、文本、图片、视频和音频直接查看。
- 选择模型和一个可选专业 Skill。
- 上传当前模型支持的附件。
- 每轮可选择直接回答，或由模型根据当前对话自主查询 Wiki 后回答。
- 明确同意后，把确认内容写入指定或确认的 Wiki 路径。
- 用自然语言生成图片、视频或音频，并在对话内确认费用、查看进度和结果。
- 从设置按钮登录、配置 Provider、管理 Skill、连接 MCP 和查看通用设置。

### 3.2 明确不做

- 不显示文、武、创、道四种模式。
- 不使用 OpenCode Agent、`ses_*`、sidecar 或 OpenCode 会话数据库。
- 不在工作台内部建立第二套项目选择器、第二份对话存储或第二份对话索引；应用级项目入口负责本机/云端项目，顶部对话选择器只负责当前项目内的对话导航。
- 不把所有文件渲染成聊天消息。
- 不建立向量库、隐藏摘要、第二套记忆或跨对话隐式拼接。
- 不自动把讨论、猜测或草稿写入 Wiki。
- 不加载 `CreationPanel`、画布、编辑区、画廊或媒体工作台。
- 不新增媒体 API、任务 Store、轮询器或音频专用流程。
- 不做媒体、Skill、MCP 凭据、Provider 或界面设置的跨设备同步；第四阶段只同步项目文字。
- Mobile 已使用独立 Bundle ID `com.jiucaihezi.mobile` 和 Apple Team；当前不创建新域名、Deep Link 或桌面式更新通道。

## 4. 唯一真源

| 内容 | 唯一真源 |
| --- | --- |
| 完整对话 | 当前项目 `.raw/对话记录/*.md` |
| 已确认知识 | 当前项目现有 `wiki/` 或 `docs/wiki/` |
| 当前打开资源 | 页面存活期间的 UI 状态；重启后从最近项目和文件树恢复 |
| 项目文件 | `ProjectFileService` 背后的当前平台存储 |
| 跨设备文字副本 | Cloudflare `SYNC_DB`；只接受已登录账号名下项目，不取代本地项目文件 |
| 模型与 Provider | 现有公共模型/Provider 配置 |
| 产品内置 Skill | `public/skills/` 与现有只读 loader；不进入仓库或选择器 |
| 用户已安装 Skill | Web `agentStore` 的 `jc_web_skills_v1`；与内置 Skill 合并为模型可发现目录 |
| MCP | 现有 `mcpStore`、`mcpClient` 与安全凭据存储 |
| 生成媒体 | 当前项目 `jc-media/` |
| 在途媒体任务 | 现有 `mediaTaskStore` |

App 不再把同一份对话正文保存进 conversations/messages 数据库。清除页面状态后，只要项目目录还在，就必须能从 Raw 恢复全部对话，从 Wiki 恢复确认知识，从 `jc-media/` 恢复已生成媒体。第四阶段的 D1 是跨设备传输和恢复副本，不是模型直接读取的新记忆库；同步下载后仍由 `ProjectFileService` 读写本地文件。

## 5. 项目文件合同

### 5.1 最小骨架

```text
项目根/
├── .raw/
│   └── 对话记录/
│       ├── chat_01J0A7F3.md
│       └── chat_01J0B2K9.md
├── jc-media/                 # 首次生成媒体后按需创建
│   ├── images/
│   ├── videos/
│   └── audios/
└── wiki/                     # 已有 docs/wiki/ 时接管原位置
    ├── CLAUDE.md
    ├── index.md
    ├── hot.md
    ├── log.md
    └── 来源索引.md
```

新建通用 Wiki 不预设“资料、主题、参考”等业务目录。用户需要什么，模型在用户确认后创建什么。已有 Wiki 只接管，不迁移、不删除、不覆盖。

### 5.2 对话身份

对话文件使用稳定 ID 文件名，首个 H1 是用户看到的标题：

```markdown
# 聊聊历史

<!-- jc:conversation id="chat_01J0B2K9" created-at="2026-07-24T15:00:00+08:00" -->

<!-- jc:turn id="turn_01J0B31A" role="user" created-at="2026-07-24T15:01:00+08:00" -->
## 用户

秦朝为什么灭亡得这么快？
<!-- /jc:turn -->
```

一个文件只有同时满足以下两项才是对话：

1. 路径位于 `.raw/对话记录/`；
2. 文件内容含可解析的 `jc:conversation` 元数据标记。

普通 `.md` 文件不能只因扩展名相同就成为对话。路径正确但标记缺失、标记存在但路径错误，都按普通文档打开；不得猜测或自动改写。

### 5.3 Raw 边界

Raw 只追加用户可见内容：

- 用户输入和模型完整最终回复；
- 用户可见附件的项目相对路径、名称和类型；
- 用户发送该轮使用的 `quick | memory` 模式；
- 已确认媒体计划及最终成功、失败或取消结果；
- 用户确认了哪项 Wiki 写入。

Raw 不保存系统提示词、隐藏推理、模型内部加载的产品 Skill 正文、Wiki 查询结果、工具参数、工具回执、流式片段、轮询快照、鉴权地址或附件二进制副本。用户明确要求创建并用于安装确认卡的 `jc-skill-install` 块属于用户可见回复，随该轮 Raw 保存。

### 5.4 项目级附件引用

用户上传或粘贴附件时，附件不再只存在于当前页面内存：

1. 第一次出现附件时按需创建 `jc-media/uploads/`，并将原文件只写入一次；新建空记忆空间时不预建空目录。
2. Raw 附件元数据保存 `id / name / mime / size / kind / projectPath`；`projectPath` 必须是 `jc-media/` 下不含 `.`、`..` 或空路径段的项目相对路径。
3. Raw 不保存 Base64、Blob URL 或远程鉴权 URL；项目文件是附件二进制唯一真源。
4. 每轮请求从当前对话 Raw 重建历史图片和视频的受控 `referenceId` 清单。当前轮附件默认显式选中；历史附件只作为候选，由模型按用户指代写入 `referenceIds`。
5. “这张图 / 之前那张图”只有一个同类候选时可直接复用；多个候选且上下文不能唯一确定时必须询问，不得静默选择旧图。
6. 媒体确认卡持有项目定位器。切换模型、比例、分辨率、时长或再次生成，只修改任务参数，不清空素材引用；刷新或重开项目后按 `projectPath` 重新读取。

这是一套 Wiki 项目文件系统内的稳定引用关系，不是第二套附件数据库：Raw 记录附件在哪轮出现，`jc-media/uploads/` 保存附件本体，媒体计划保存受控引用。

## 6. 文件树与中央内容区

### 6.1 导航职责

当前项目打开后，对话切换由顶部对话选择器触发，资料切换由文件树触发。两者都不创建第二份数据源。

- 桌面和大屏：左侧文件树，顶部对话选择器，右侧中央内容区。
- 手机：文件树放入抽屉，中央内容区占满屏幕。
- 设置：顶部账号/齿轮按钮打开抽屉或覆盖层；关闭后回到原资源。

文件树只负责资料导航和文件动作，不显示或切换对话 Raw。记忆空间初始化后，顶部选择器旁始终显示“新建对话”；没有项目时，中央区显示选择项目文件夹的空状态。

### 6.2 统一打开路由

| 路由结果 | 识别规则 | 中央区行为 |
| --- | --- | --- |
| `conversation` | `.raw/对话记录/` + 有效 `jc:conversation` | 完整聊天记录、模型/Skill、附件、输入框 |
| `document` | 可安全完整读取的 Markdown/文本 | 只读文档视图；Markdown 渲染，纯文本保留格式 |
| `media` | 图片、视频或音频 MIME/扩展名 | 图片预览、视频播放器或音频播放器 |
| `binary` | 其他格式、超大或含 NUL 的内容 | 文件信息与安全打开、另存为或下载 |

`.jccanvas` 在本产品没有画布承载面，按不支持的结构化文件处理，只提供安全操作，不加载创作面板。

### 6.3 对话在文件树中的显示

- 文件真实路径和稳定 ID 不变，但 `.raw/对话记录/` 默认作为系统目录从资料文件树隐藏。
- 顶部对话选择器读取 `.raw/对话记录/` 下有效对话的首个 H1、稳定 ID 和更新时间，显示用户标题而不是 `chat_*.md`。
- 选择器内部 key 仍是 `owner + path`，标题只是显示别名；不建立标题数据库。
- 设置中的“显示系统文件”可让高级用户查看 Raw 原文，但查看 Raw 不改变当前对话，普通文档预览规则仍适用。

### 6.4 新建、重命名、删除与恢复

新建对话：

```text
点击顶部对话选择器旁“新建对话”
  -> ProjectFileService 创建稳定 ID Raw
  -> 写入 H1 和 jc:conversation 标记
  -> 文件树选中该文件
  -> 中央区进入空对话
```

首条消息可直接取首段文本作为标题，不调用额外模型。用户重命名对话时只修改首个 H1，不修改稳定文件名或 conversation ID。

删除仍走现有文件删除确认和 `ProjectFileService` 事件；当前对话被删除后回到空状态或打开剩余最近对话，不保留隐藏副本。刷新、重启或清除 UI 状态后，App 重新扫描 Raw 文件恢复对话；已经删除的 Raw 只能从用户自己的项目备份或导出恢复。

普通文档的重命名仍是文件路径重命名，不能套用“只改 H1”的对话规则。

### 6.5 对话选择器与统一预览层

#### 对话选择器

顶部固定显示当前对话标题、下拉箭头和“新建对话”：

```text
[ 当前对话：你好 ▾ ]  [＋ 新建对话]                       [模型] [设置]
```

展开后提供当前项目的对话搜索和最近对话列表。选择对话时读取对应 Raw，恢复完整聊天、最后一次 `快速 | 记忆` 状态、输入草稿和附件；不调用额外模型，不复制正文。

选择器支持新建、切换、重命名和删除。删除当前对话后打开剩余最近对话；没有剩余对话时显示新建入口。对话标题修改只写 Raw 首个 H1，稳定文件名和 ID 不变。

#### 统一预览层

从文件树打开任何非对话资源，都在当前对话之上打开临时预览层，不销毁或替换对话组件：

```text
当前对话
  -> 点击文件
  -> 预览层覆盖当前对话
  -> 返回 / Esc / 关闭
  -> 原对话、滚动位置、草稿和附件完整恢复
```

预览层顶部统一为：

```text
← 返回对话    文件名                              下载 / 编辑 / 关闭
```

图片、视频、音频、Markdown、纯文本、PDF 和其他安全文件使用各自阅读器，但共享返回、下载和关闭合同。点击预览层内另一文件只替换当前资源，不改变返回目标。

普通文件默认只读；只有用户点击“编辑”才进入现有编辑器。`.raw/对话记录/` 是唯一例外：通过顶部选择器切换对话，不从普通预览层进入聊天。

运行时最小状态：

```ts
activeConversationId: string
previewResource?: ProjectResource
previewReturn: { conversationId: string; scrollTop: number; draft: string; attachments: Attachment[] }
```

`previewReturn` 只保存页面存活期间的 UI 状态，不写入 Wiki、Raw 或第二套存储。

## 7. 对话运行合同

### 7.1 正确时序

```text
用户点击发送
  -> App 把用户消息及 quick | memory 模式追加到当前 Raw
  -> App 读取当前对话 Raw
  -> 快速：当前对话 + 本轮消息/附件，无 Skill/Wiki/MCP 工具
  -> 记忆：当前对话 + 本轮消息/附件 + Wiki/Skill/MCP 工具
  -> 模型按 Skill description 自主判断是否调用 skill(name) 加载 SKILL.md
  -> 模型按任务需要决定是否查询 Wiki 或调用其他工具
  -> 模型生成最终回答
  -> App 把完整最终回答追加到当前 Raw
```

App 不在模型之前猜关键词，不提前查询 Wiki，不把固定查询结果拼进请求。最新用户消息只出现一次。

`快速` 和 `记忆` 共用同一份 Raw；开关只影响下一次发送。新对话默认 `记忆`，重开对话恢复最后一个带模式的用户 turn。用户要求修改当前回答时，当前 Raw 全文和最新意见仍进入同一个模型请求。

### 7.2 Skill 调用条件

记忆模式遵循官方 Skill 渐进披露：首轮只提供可用 Skill 的名称和 description；模型判断任务匹配后调用统一的 `skill(name)` 加载对应 `SKILL.md`，再按 Skill 指引使用项目工具。任务不匹配任何 Skill 时直接回答，不强制加载 Skill，不强制查询 Wiki，也不设置回复出口门禁。App 不做关键词路由，不把任何 Skill 正文预塞进普通请求。

输入框的 `@` 是统一的本轮引用入口，动态列出当前项目可用文件和用户已安装 Skill。选择项目文档后读取真实内容作为本轮 `files` 引用；选择项目媒体后复用现有附件物化合同；选择 Skill 后自动进入记忆模式，并在首轮模型请求之前用标准 assistant tool call + tool result 协议精确执行 `skill(name)`。这是用户明确选择，不是关键词路由，也不把产品内置 Skill 暴露到选择器。切换到快速模式必须清除已选 Skill。

快速模式保持纯直连，不提供 Skill、项目或 MCP 工具。不支持工具调用的模型可以用于快速模式，但不能进入记忆模式伪装成具有 Skill 能力。

### 7.3 上下文

- Raw 永远保存当前对话完整原文。
- 每轮先读取当前 Raw，不创建隐藏摘要或第二套记忆。
- 请求沿用现有模型上下文装配；超出模型容量时只在请求边界保留最新完整 turns，不修改 Raw。
- 不阻止用户继续发送，也不增加专用“对话过长”流程。
- 跨对话知识只通过 Wiki 查询取得，不自动拼入其他对话。
- Provider 返回容量错误时如实显示，不伪造回答，不写 assistant Raw。

## 8. Wiki 与 Skill

### 8.1 系统 Skill

| 动作 | 自动使用 |
| --- | --- |
| 新建或接管 Wiki | `jc-everything-wiki` |
| 用户任务需要查询长期记忆 | `jc-cha-wiki` |
| 用户确认写入新知识 | `jc-raw-wiki` |
| 用户确认修正错误 | `jc-xiu-wiki` |
| 用户要求巡检 | `jc-jian-wiki` |

这些系统 Skill 与 `skill-creator`、`jc-new-user-guide` 等产品能力继续放在 `public/skills/`，由产品或模型按动作调用，不进入 Skill 仓库和 Skill 选择器。`public/skills/index.json` 只服务内部路由，不再作为用户 Skill 列表。

### 8.2 用户 Skill

Skill 仓库与输入框 `@` 菜单都读取 `agentStore.getCustomSkills()`；新增、编辑和删除后，记忆运行时下一轮生成的 Skill 目录及 `@` 菜单立即同步。仓库和 `@` 菜单都不读取或展示 `public/skills/index.json`。

Web 用户 Skill 写入浏览器可写存储 `jc_web_skills_v1`。不得把 `public/zijian/skills/` 设计成用户安装真源，因为 `public/` 在发布后是只读静态资源，浏览器无法把运行时生成的 Skill 写回该目录。Desktop 阶段复用现有 Central Skill 用户目录 `~/.agents/skills`。内置 Skill 优先解决同名冲突，用户 Skill 补充同一模型目录。

用户已安装 Skill 与产品内置 Skill 使用完全相同的自动发现合同：名称和 description 进入目录，正文只在模型调用 `skill(name)` 后加载。额外允许用户从 `@` 菜单明确选择已安装 Skill；该动作只对本轮生效并精确执行同一个 `skill(name)`，不增加关键词命中或固定 Skill 绑定。

`skill-creator` 生成的 Skill 只有在用户明确说“安装”后，才输出包含完整单文件 `SKILL.md` 的 `jc-skill-install` 块。界面把它解析为“安装到我的 Skill / 继续修改”确认卡；只有用户点击安装后才调用现有 `createAgent()` 写入用户 Skill Store，不能静默安装。第一版不安装 `references/`、`scripts/` 或 `assets/`；需要这些资源时必须先把必要规则收进自包含 `SKILL.md`。仓库现有手动自建入口继续保留。

### 8.3 Wiki 写入

普通对话只提供 Wiki 只读工具。只有以下情况进入受限写入轮次：

- 用户明确要求写入或修改 Wiki；
- 用户指定路径；
- 用户确认模型建议的路径和内容；
- 用户通过现有工具审批。

未指定路径时，模型先查询现有 Wiki，优先复用已有页面；没有合适位置时提出一个最小新路径，等待确认。写入后更新必要入口、`hot.md`、`log.md` 和 `来源索引.md`，重要结论可追溯到 Raw 文件及 turn 范围。

## 9. MCP 与设置

### 9.1 隐藏设置

设置不是第三栏，也不是主导航。顶部账号/齿轮按钮打开覆盖层或抽屉，V1 只包含：

1. 登录、退出、会员/余额、Provider、API Key 和模型刷新；
2. Desktop 的现有本地模型能力；
3. Desktop `CentralSkillsPanel` / Web `WebSkillPanel`；
4. `McpManagerPanel`；
5. 同步总开关、当前状态和诊断；云项目选择、首次上传和下载位于应用级项目入口；
6. 外观、语言、版本、更新和关于。

不带插件管理、OpenCode 升级、变更审查、创作偏好或工作台专属设置。

### 9.2 MCP

已启用且已连接的 MCP 工具经现有 `mcpStore -> mcpBridge` 进入同一模型工具循环，由模型按本轮目标决定是否调用。

Desktop 支持现有本地 `stdio` 与远程 MCP；Web 只显示浏览器真实可执行的远程 MCP。凭据、OAuth token、工具参数和完整回执不进入 Raw 或 Wiki。

## 10. 对话内媒体

媒体生成是基础能力，不依赖创作面板或特定 Skill。用户要求生成图片、视频或音频时：

```text
模型按当前快速/记忆合同回答
  -> 上传附件先写入当前项目 jc-media/uploads/
  -> 当前 Raw 记录项目相对路径
  -> 本轮及历史图片/视频附件注册为受控 referenceId
  -> 生成公共 MediaPlan(image | video | audio)
  -> 按需展开现有 CreationPanel 并装载模型、提示词、参数和素材
  -> 用户在 CreationPanel 点击“开始生成”
  -> preparePublicMediaPlan()
  -> mediaTaskStore.submitTask()
  -> CreationPanel 显示进度、取消、失败和历史
  -> 结果写入当前项目 jc-media/
  -> 对话与 Raw 只追加最终回执和项目相对路径
```

- 三种媒体都使用现有模型注册表、公共计划、`CreationPanel`、任务 Store 和轮询恢复。
- 单个媒体任务使用一个计划对象；同一轮包含多个独立媒体任务时，`jc-media-plan` 使用对象数组并以“回复轮次 + 计划序号”隔离草稿。解析成功后技术 JSON 块不进入可见正文；解析失败时保留原文，不静默隐藏错误。
- 聊天模型看懂附件不等于媒体模型收到附件；计划必须经 `buildExplicitMediaReferences -> materializeMediaPlanReferences` 物化后装载到创作面板，才能进入图生图或图生视频。
- 历史上传附件从 Raw 的 `projectPath` 重建项目定位器；付费提交前重新读取项目文件，不依赖首次上传页面尚未释放的 Data URL。
- 音频按现有模型字段进入 `buildCreationRunPlan()` 和 `audioParams`，不另造音频合同。
- 所有可能扣费的任务必须先由用户确认。
- 不持久化临时远程 URL；成功结果先落项目，再写 Raw。
- 成功回执以项目文件为真源，显示 `已保存到 jc-media/...`，并只提供预览、下载副本和在文件树中查看；不再显示含义重复的“保存”。
- Raw 只写成功任务的项目相对路径，不写 CDN URL；远程 URL 只是内部下载源和旧任务降级通道。
- 媒体计划的生成按钮、任务图标、进度条和操作悬停色统一使用 `--olive*` 主题变量，不写死紫色。
- 任务状态库丢失时，已成功媒体仍可从 Raw 相对路径和 `jc-media/` 恢复。
- 媒体意图出现后按需加载现有 `CreationPanel` 与画布；默认对话工作区不预加载它们，也不建立第二套媒体引擎。

### 10.1 创作面板融合决策（2026-07-27）

对话媒体确认卡与主 App `CreationPanel` 已经共享模型注册表、计划物化和 `mediaTaskStore`，不得再建立第二个媒体工作台。记忆 App 的媒体交互改为：

```text
模型返回 MediaPlan
  -> 立即按需展开现有 CreationPanel，并装载提示词、模型、参数和项目素材引用
  -> 用户在 CreationPanel 明确点击生成
  -> mediaTaskStore 冻结任务快照、轮询并落当前项目 jc-media
  -> 进度与历史只在 CreationPanel 显示
  -> 对话只追加最终成功/失败回执和项目相对路径
```

1. `CreationPanel` 是可收起的同应用专业区域，不是独立 App；用户可在不继续对话时直接填写提示词、选模型和连续生成。
2. 对话上传、创作面板导入、文件树引用和画布节点不得各自持有媒体副本。项目文件是唯一真源，其他位置只保存 `{ owner, path, id? }` 引用。
3. 创作面板“添加参考素材”复用现有画布导入：文件先写入项目，再加入画布并选中为本次参考；不得扩建 `File[] / Data URL` 为持久附件系统。
4. 创作面板输入 `@` 时只搜索当前项目支持的图片、视频和音频；选中项加入当前画布并成为参考素材，不复制对话输入框中的 Skill、Agent 或 MCP 菜单。
5. 所有可能扣费的任务继续由用户在创作面板明确点击生成；模型装载计划不能自动提交。
6. 生成结果默认只进入任务历史和项目 `jc-media`，不自动追加到画布。用户点击“放到画布”后才按媒体真实宽高比建立节点。
7. 对话不显示完整计划编辑器或轮询卡；成功回执只保留预览、下载副本和在文件树中查看，原始链接等低频动作进入更多菜单。
8. `CreationPanel` 首次媒体意图后才异步加载并保持挂载；关闭只隐藏，不丢当前项目草稿、任务和画布。切换项目必须切换对应文件真源，不能把旧项目引用带入新项目。

### 10.2 创作面板布局与项目资源交互收尾（2026-07-27）

记忆工作台不另建布局、预览器或画布导入引擎，直接复用主 App 的可调分栏、项目资源预览和 `CreationPanel.addMediaToCanvas()`：

1. Desktop/Web 宽屏使用“文件树 280px + 对话自适应 + 创作面板”三栏。创作面板默认占可用宽度约 33%、最小 520px，对话最小 420px；两者之间复用主 App 指针拖拽分隔逻辑并持久化用户宽度。首次展开时若空间不足，优先收起文件树，不把对话压成不可读窄栏。
2. Mac 原生标题区固定 28px，文件树、对话区和创作面板工具栏统一 52px；三栏下沿使用同一边线基线。专注模式在 Desktop 仍保留 28px 原生标题区。
3. 创作面板工具栏保留定位画布、新建画布、历史、提示词参考、专注创作和收起面板六个一级操作；不收入更多菜单。“新建项目文档”属于文件树职责，不在创作面板重复提供。
4. 任务历史“预览”与文件树左键共用项目资源预览入口：先关闭历史弹层，再在对话区打开图片、视频或音频，并保留“返回对话”。只有未成功写入项目的结果才使用原始链接兜底。
5. 任务历史“放到画布”、文件树右键“加入画布”和文件树拖入画布共用同一项目媒体入口。记忆模式下右键动作不得经过普通文件预览的提前返回；内部拖拽必须消费项目资源引用，不只读系统 `FileList`。
6. 对话区复用主 App 的可拖动竖向滚动条与粘性跟随策略：用户在底部时流式内容自动跟随，用户主动向上阅读时停止抢滚，回到底部后恢复跟随。

## 11. 最小实现架构

### 11.1 复用现有产品壳，不创建 `apps/chat`

当前阶段直接在现有 Web/Desktop 产品身份内实现“记忆对话工作台”入口和布局，不创建平行 App 工程。

最小新增边界：

| 模块 | 责任 |
| --- | --- |
| `ConversationSwitcher` | 从 `.raw/对话记录/` 投影对话列表，切换、创建、重命名和删除对话 |
| `CentralContentHost` | 保持当前对话，并在统一预览层显示文档、媒体或安全文件视图 |
| `ConversationTranscript` | 创建、识别、解析、追加和重命名对话 Raw |
| `MemoryChatRuntime` | 当前 Raw 请求装配、Wiki/Skill/MCP 工具循环和回复落盘 |

名称可按现有代码风格调整；不为了对齐本文先创建空目录或抽象层。

### 11.2 共享资源打开链路

实现只扩展现有合同：

```ts
type ProjectResourceOpenResult =
  | { type: 'conversation'; resource: ProjectResource; transcript: ConversationTranscript }
  | { type: 'editor'; resource: ProjectResource; text: ProjectTextRead; editorMode: 'rich' | 'plain' }
  | { type: 'unsafe-text'; resource: ProjectResource }
  | { type: 'canvas'; resource: ProjectResource }
  | { type: 'media'; resource: ProjectResource; mediaKind: 'image' | 'video' | 'audio' }
  | { type: 'binary'; resource: ProjectResource }
```

`openProjectResource()` 对文档完成安全读取后，再用“路径 + 元数据”识别对话。普通资源分类仍沿用现有 `ProjectResource`，不把需要读正文才能确定的 conversation 硬塞进扩展名分类。

### 11.3 直接复用

- `ProjectFileService` 与 Desktop/Web 项目适配器；
- `openProjectResource()` 统一路由与资源变更事件；
- 现有账号、Provider、模型和安全凭据能力；
- Skill loader、自动发现目录与双端 Skill 仓库；
- `McpManagerPanel`、`mcpStore`、`mcpClient` 与 `mcpBridge`；
- Direct Engine、附件和 Markdown 渲染的底层能力；
- `MediaPlanCard`、`MediaTaskBubble`、`preparePublicMediaPlan()` 与 `mediaTaskStore`；
- Web 的 IndexedDB + OPFS 项目存储和项目相对媒体路径。

只复用现有聊天 UI 的气泡、输入框和必要样式，不复用其 OpenCode、四模式或会话数据库逻辑。

## 12. 跨端布局与存储

### 12.1 Web

- 首发地址固定为 <https://jiucaihezi.studio>。
- 复用当前 Origin、账号回调、IndexedDB + OPFS 项目存储和发布流程。
- 不建立 `chat.jiucaihezi.studio` 或新的 Pages 产品。
- 正式 Web 发布必须在 Mac 私测和 Web 预览验收通过后进行；预览使用同一 Pages 项目的预览部署，不切换正式域名。

### 12.2 Desktop

- 第二阶段继续使用现有“韭菜盒子”产品名、Bundle ID、签名、Deep Link 和更新通道。
- 不建立新安装身份，不要求用户并装第二个客户端。
- 现有主 App 源码、发布记录和可回退版本继续保留；新工作台验证失败可按现有版本回滚。
- Mac 私测先于 Web 正式发布；在 Desktop 适配完成前，现有 Mac 构建仍是旧 `WorkspaceLayout`，不能作为新工作台验收证据。
- Mac 私测使用本机调试/测试构建，不触发正式更新通道；真实 RH 3D 验收须在新版 `rh-adapter` 和 NewAPI 模型配置完成后进行。

### 12.3 Mobile

- 第四阶段先完成 Web、Mac 与 Mobile 共用的文字同步，再做手机壳；不先制造第二个数据孤岛。
- Mobile 使用 Tauri v2 的 iOS/Android 能力和现有 Vue 运行时，不新建另一套 React Native/Flutter 业务代码；先在真实 iPhone 闭环，再补 Android。
- 使用同一 Raw、Wiki 和 `ProjectFileService` 合同；移动端增加 App 管理目录适配器，不依赖任意系统目录权限。
- 项目存放在 App 管理的可写目录；文件树读写的是该项目目录，不依赖任意系统目录权限。
- 支持系统文件导入、导出和分享；登录同一韭菜盒子账号后，可以拉取云端文字项目并继续对话。
- 手机只改变布局：文件树抽屉 + 中央内容区 + 设置抽屉，不改变模型和记忆链路。

### 12.4 同账号文字云同步

#### 产品边界

同步的不是“聊天数据库”，而是项目里的安全文本文件：

| 内容 | 是否同步 | 说明 |
| --- | --- | --- |
| `.raw/对话记录/*.md` | 是 | 对话标题、用户/助手 turn 和媒体项目相对路径一起同步 |
| `wiki/**`、`docs/wiki/**` | 是 | 否则手机与 Mac 的长期记忆会分叉 |
| 普通 Markdown、纯文本、JSON、YAML、CSV 等安全文本 | 是 | 使用扩展名/MIME 白名单和单文件大小上限 |
| `jc-media/**` 图片、视频、音频、3D、上传附件 | 否 | 只留在生成或上传它的设备 |
| 用户 Skill、MCP、Provider、API Key、Session、界面设置 | 否 | 继续使用各平台现有安全存储；凭据绝不进入同步库 |
| 二进制、未知格式、`.env` 和其他敏感文件 | 否 | 不读取、不上传、不猜测 |

另一设备打开含媒体引用的 Raw 时，正常显示文字和一张“媒体仅保存在原设备”的占位卡，不把缺失的 `jc-media/...` 当作损坏项目，不自动访问历史临时 URL。用户可在当前设备重新上传或重新生成媒体。

#### Cloudflare 边界

可以并且应该使用 Cloudflare D1，但不改 NewAPI：

```text
Web / Mac / Mobile
  -> 现有 api.jiucaihezi.studio Gateway
  -> requireWebUser() 校验现有韭菜盒子 Session
  -> /sync/projects 与 /sync/projects/:id/files
  -> 独立 D1 binding: SYNC_DB
```

- 复用现有 Gateway、`jc_session` / `X-JC-Session` 和 NewAPI 账号 ID，不再注册第二个账号。
- 新建独立 `SYNC_DB`，不把大量项目正文混入现有登录数据库 `DB`；同一个 Worker 绑定两个 D1 即可。
- 不启用 R2。只有未来明确要求媒体跨设备时，才单独评估 R2、流量、生命周期和用户配额。
- 所有查询必须带 `user_id` 所有权条件；客户端提交的 user ID 不可信，用户身份只能来自服务端 Session。
- Worker 只接受允许的相对路径、UTF-8 文本、大小上限和内容哈希；拒绝 `..`、绝对路径、NUL、凭据路径和媒体目录。
- 第一版服务端合同为 `GET/POST /sync/projects`、`GET/POST /sync/projects/:id/files`、`POST /sync/projects/:id/delete` 与 `POST /sync/projects/:id/restore`。单文件上限 2MB，单批 1-100 项，单次拉取最多 200 个路径。

最小数据模型：

```sql
projects(id, user_id, name, created_at, updated_at, deleted_at)
text_files(project_id, path, content, content_hash, revision, updated_at, deleted_at)
sync_mutations(id, project_id, device_id, created_at)
```

`projects(user_id, updated_at)`、`text_files(project_id, path)` 和 `sync_mutations(id)` 建唯一/查询索引。`mutation id` 保证网络重试不会重复写入；`revision` 是乐观并发条件，不以客户端时钟判断谁覆盖谁。

#### 同步时序

本地项目文件始终先落盘；同步失败不能阻止离线聊天：

```text
本地创建/修改/删除安全文本
  -> ProjectFileService 成功
  -> 写入本地待同步队列（mutation id + expected revision）
  -> 在线时按路径顺序、每批最多 100 项推送到 Gateway
  -> D1 原子校验账号、路径、mutation id 和 revision
  -> 返回新 revision / 冲突

登录、打开项目、App 回到前台、用户点“立即同步”
  -> 按 cursor 拉取远端变更
  -> 写入本地 ProjectFileService
  -> 更新本地 cursor
```

第一版不做 WebSocket、字符级实时协同、后台常驻进程或 CRDT。产品目标是“手机用完，Mac 打开后接着用”，不是两台设备同时编辑同一行。

#### 冲突与删除

- 服务端更新必须携带 `expectedRevision`；不一致返回 `409`，绝不静默最后写入覆盖。
- Raw 对话仅在能按稳定 turn ID 无歧义合并时自动合并；否则保留本地版本，并生成可见冲突副本等待用户选择。
- Wiki 和普通文档第一版不做语义自动合并；冲突时保留两份，模型不能替用户猜哪份正确。
- 删除使用 tombstone，至少保留 30 天，防止离线设备把旧文件重新上传；恢复和永久删除后续复用同一 revision 合同。
- 项目列表、文件正文和 tombstone 都按账号隔离；退出登录后停止同步，但不删除用户本地项目。

#### 项目身份

D1 为每个云项目生成稳定 `project_id`。每台设备只在本地保存 `本地 owner -> cloud project_id + cursor` 映射；文件路径仍由 `ProjectFileService` 管理，不把绝对路径上传云端。新设备从云项目列表选择项目后，在 App 管理目录创建本地副本，再进入现有工作台。

#### 云项目产品入口

云项目是正式的项目来源，不是藏在设置页里的高级同步选项。应用级项目入口统一提供：

- `本机项目`：打开当前设备已有项目。
- `云端项目`：列出当前账号名下未删除的云项目。
- `新建项目`：在当前设备创建本地项目。
- `打开本地文件夹`：Desktop 选择已有目录；Web 继续使用浏览器项目存储。

首次动作只有两种：

1. `上传到云端`：为当前本地项目创建稳定 `cloud project_id`，上传允许同步的完整文字快照，并保存本地映射。
2. `下载到本机`：用户从云项目列表选择项目；Web 创建浏览器本地副本，Desktop 选择保存位置并创建同名项目目录，Mobile 写入 App 管理目录；完整拉取成功后保存本地映射并打开项目。

完成首次上传或下载后，后续只有 `立即同步`：先按 cursor 增量拉取，再按本地待同步队列增量推送。不得把“再次下载”实现为删除本地目录或用云端快照静默覆盖；同路径双方均有修改时继续使用 revision 冲突与可见冲突副本合同。

“立即同步”是用户手动强制执行或失败重试的入口，不是日常保存按钮。项目完成首次绑定后，本地文本成功写入即自动进入待同步队列并尝试上传；打开项目、App 回到前台或网络恢复时自动执行一次先拉后推。断网时本地保存仍须成功，恢复后沿用原 mutation id 继续。

项目入口显示云端状态和原生进度条：`正在扫描 N/M / 上传 N/M / 下载 N/M / 同步完成 / 待同步 N 项 / 同步失败 / 存在冲突`，并固定说明“只同步文字，媒体和空目录不同步”。设置页复用同一同步状态，只保留账号、状态、进度和诊断，不承担项目配对。旧的“先绑定本地文件夹，再在设置中连接已有云项目”交互已移除。

同一云项目在不同设备拥有不同本地 owner 是正常状态：

```text
Web 浏览器项目 owner ─┐
                      ├─ cloud project_id
Mac 本地目录 owner ───┤
Mobile App 目录 owner ─┘
```

项目名称用于展示，不作为身份；重名云项目必须使用稳定 ID 区分。删除本地副本不等于删除云项目；删除云项目必须单独确认并沿用 tombstone/恢复合同。

## 13. 错误与并发

| 情况 | 行为 |
| --- | --- |
| 用户消息 Raw 追加失败 | 不发模型请求，保留输入并允许重试 |
| 对话读取失败 | 不请求模型，显示真实文件错误 |
| Wiki 查询失败或未发生 | 不接受候选回答，不追加 assistant |
| 模型失败、取消或流式中断 | 用户消息保留，不写不完整 assistant |
| assistant Raw 追加失败 | 保留可见结果并阻止下一轮，直到落盘成功 |
| Wiki 审批拒绝或写入失败 | 不回滚 Raw；拒绝则不写，失败可按相同来源重试 |
| 媒体失败或取消 | Raw 追加最终状态，不写成功路径 |
| 媒体成功但项目落盘失败 | 保留生成成功与原始结果，单独显示保存警告和“重试保存” |
| MCP 未连接或调用失败 | 不伪造外部结果，显示真实错误 |
| 多窗口同时追加 | revision 冲突后重读，按 turn ID 去重重试 |
| 文字同步离线或超时 | 本地写入照常成功，保留待同步状态；恢复网络后按同一 mutation id 重试 |
| 云端 revision 冲突 | 返回 409，保留两份并显式提示；禁止最后写入静默覆盖 |
| 另一设备缺少媒体 | 文字继续可读，媒体显示“仅保存在原设备”，不把缺失媒体当成同步失败 |
| Session 失效 | 停止同步并要求重新登录，不删除本地文件、不退化为公共数据 |
| Provider 容量错误 | 原样显示，不新增隐藏摘要流程 |

## 14. 实施顺序与发布门槛

### 第一阶段：Web 公共底座（已完成本地实现）

1. 修正通用 Wiki 最小骨架和 Raw 来源合同。
2. 在 `openProjectResource()` 增加对话识别与打开结果。
3. 建立文件树 + `CentralContentHost`，完成四种资源显示。
4. 实现 Raw 新建、标题显示、聊天恢复、追加、重命名和删除。
5. 接入 Direct Engine、官方式 Skill 自动发现、用户 Skill、确认写入和 MCP。
6. 接入隐藏设置及对话内图片/视频/音频能力。
7. 保持正式域名不变，先完成本地自动化验证；正式域名发布移到第三阶段。

验证：进入本地 Web 项目后无需工作台内模式或重复项目选择器；通过顶部对话选择器切换对话，通过文件树打开资料预览、查询和写入 Wiki，并完成公共媒体计划验证。

### 第二阶段：现有 Desktop App / Mac 私测（已完成 Apple Silicon 主路径）

1. 将第一阶段公共运行时与布局接入 Desktop，替换旧 `WorkspaceLayout` 的工作台入口。
2. 接入现有 Desktop 项目文件、Keychain、本地模型、本地 MCP 和系统打开能力。
3. 先生成本机 Mac 测试构建，不上传商店、不触发正式更新通道。
4. 部署新版 `rh-adapter`，在 NewAPI RH 渠道加入 `rh-3d-text`、`rh-3d-image` 及对应价格。
5. 在 Mac 测试版完成 Raw、Wiki、Skill、MCP、媒体和混元 3D 真实闭环。

验证：Apple Silicon Mac 主路径与 2.0.1 本机包已完成；Intel Mac 和 Windows 仍属于后续平台矩阵。测试失败可直接回退现有主 App，不影响正式 Web 用户。

### 第三阶段：Web 预览与正式发布

1. 用第一阶段 Web 构建部署到同一 Cloudflare Pages 项目的预览地址。
2. 用真实浏览器验收登录、项目文件树、Raw 对话、Wiki、Skill、MCP、媒体和手机宽度布局。
3. 预览验收通过后，构建正式 `dist/` 并发布到 <https://jiucaihezi.studio>。
4. 保留旧 Pages 部署作为回滚版本；不修改产品域名、账号回调或存储合同。

验证：正式 Web 用户可以完成完整成功路径，且 Mac 私测与 Web 预览没有未关闭的阻断问题。

### 第四阶段：文字同步底座与 Mobile App

#### 4A：同步服务与云项目入口

1. 在 Cloudflare 新建独立 D1 `SYNC_DB`，提交可审计 migration，并绑定现有 Gateway。
2. 在 Gateway 增加受 `requireWebUser()` 保护的项目列表、增量拉取、批量推送和删除恢复接口；先写越权、路径、幂等与 revision 冲突测试。
3. 建立客户端 `syncClient + 本地待同步队列`，只消费 `ProjectFileService` 成功后的文字变更，不直接接管底层文件系统。
4. 把云项目并入应用级项目入口，完成“本地项目上传到云端”和“云项目下载为新本地项目”；设置页不再负责手工配对两个 owner。
5. 先用同一账号完成 Web 上传 -> Mac 下载、Mac -> Web 增量同步、离线重试和冲突副本真实闭环。

验证：新设备无需预先创建同名项目或理解本地 owner；从云项目列表下载后即可顺序继续同一 Raw 对话和 Wiki。断网不丢本地内容，重连不重复 turn，不同步任何媒体或凭据，不同账号无法探测项目是否存在。

#### 4B：多产品构建边界（Mobile 前置）

1. 默认 `App.vue + tauri.conf.json` 只构建记忆工作台，保留现有记忆 App 的产品身份和用户数据目录。
2. Studio 使用独立 `StudioApp.vue + tauri.studio.conf.json`，只有该目标声明 OpenCode sidecar、Studio Bundle ID、Deep Link 和更新通道。
3. Vite 在构建期静态选择产品入口，不用运行时开关，也不把其他产品壳作为隐藏 chunk 打入当前产物。
4. 正式记忆 App CI 不下载 OpenCode；Mac DMG 与 Windows 便携包必须反向断言不存在任何 `opencode` 文件。
5. 暂不为了体积给共享 Rust 命令逐项加 feature gate；先移除占体积最大的 sidecar，并以实际产物测量决定下一步，避免为了尚未证明的收益拆散稳定原生层。

验证：默认记忆 App 和 Studio 两个前端入口均能独立通过 TypeScript 与正式构建；记忆安装包内没有 OpenCode sidecar，Studio 配置仍明确携带它；现有记忆 App 升级后仍读取原数据目录。

#### 4C：Mobile 壳与移动文件适配

实施顺序：先发布 Web/Desktop v2.0.9 稳定节点，再从同一仓库建立 iPhone 独立开发分支；不把未完成的移动适配混入已发布桌面入口。

1. 审计现有 Tauri 插件的 iOS/Android 支持，只替换 Desktop 专属文件、窗口、Keychain、stdio MCP 和本地模型入口；共享 Vue 组件、Direct Engine、Raw/Wiki 和同步客户端。
2. 增加移动 `ProjectFileService` 适配器和 App 管理项目目录，从云项目列表拉取文字副本。
3. 把桌面三栏改为逐页导航：文件树和设置使用抽屉，聊天/预览占满中央内容区；不依赖悬停、右键和桌面拖放。
4. 接入移动登录回调、系统附件、媒体生成/播放和导入导出；媒体仅落手机本地。
5. 第一版只闭环登录、云项目、文字同步、对话、Wiki、系统附件、媒体任务提交/恢复和结果预览；复杂画布与完整桌面创作面板不作为首版门禁。
6. 先用 Xcode 真机构建和 TestFlight 在真实 iPhone 完成前后台恢复、断网重试及上述闭环，再补 Android 构建与权限矩阵。iPhone 后续更新由 App Store/TestFlight 管理，不复用 Desktop 下载页或 OTA 更新器。

验证：手机创建或继续项目后，Mac/Web 能取得新增文字并继续；Mac/Web 的文字也能出现在手机。媒体在原设备可用、另一设备显示明确占位；关闭重开后从本地项目恢复，并在登录联网后增量同步。

#### 4C 分项验收清单

| 编号 | 验收目标 | 通过标准 | 状态 |
| --- | --- | --- | --- |
| 4C-1 | 独立 iPhone 开发边界 | `codex/iphone-app` 从 Web/Desktop 稳定节点分出；不改 `main`、不影响 Web/Mac 正式包。 | 已完成 |
| 4C-2 | 官方 iOS 工程与交叉编译 | Tauri 官方 `ios init` 可再生工程生成；`aarch64-apple-ios` 和 Simulator target 的 `cargo check` 均通过。 | 已完成 |
| 4C-3 | Desktop 专属依赖隔离 | `rfd`、`trash` 等 Desktop-only 依赖不再进入 iOS target；不改变共享业务运行时。 | 已完成 |
| 4C-4 | Simulator 构建诊断 | 官方 debug Simulator `.app` 可构建、安装并产出启动诊断；不把“安装”当作“运行”。 | 已完成 |
| 4C-5 | 真实设备签名与安装 | iPhone 13 Pro Max 使用 `com.jiucaihezi.mobile` 和 `RXD4L9387J` 完成签名、IPA 导出、安装及启动请求。 | 已完成 |
| 4C-6 | 原生壳真机稳定启动 | 仅采用官方已发布且兼容的 Tauri/Wry 组合；真实 iPhone 冷启动进入首页，连续停留至少 60 秒且无崩溃报告。每次实验须记录版本、构建、安装、启动结果。 | 进行中 |
| 4C-7 | 移动项目目录适配 | App 管理目录的 `ProjectFileService` 可创建、读取、修改安全文本项目，无任意目录权限依赖。 | 未开始 |
| 4C-8 | 移动工作台导航 | 文件树与设置为抽屉，聊天/预览全屏；无悬停、右键或桌面拖放前提。 | 未开始 |
| 4C-9 | 移动登录与云项目 | 同账号登录回调可用，云项目可下载为本地副本并恢复项目映射。 | 未开始 |
| 4C-10 | 文字同步与离线恢复 | 手机上线后按先拉后推同步 Raw/Wiki/安全文本；离线写入、重连、冲突副本均可验证。 | 未开始 |
| 4C-11 | 移动附件与媒体 | 系统导入/导出/分享、媒体提交、播放、任务恢复可用；媒体只存手机本地。 | 未开始 |
| 4C-12 | 真机业务回归 | 真实 iPhone 验收登录、项目、对话、Wiki、附件、前后台恢复与断网重试；复杂画布不纳入首版门槛。 | 未开始 |
| 4C-13 | TestFlight 与后续平台门槛 | 4C-12 全部通过后才提交 TestFlight；TestFlight 稳定后才开始 Android 构建与权限矩阵。 | 未开始 |

4C-6 阻断规则：若官方尚未发布兼容版本，必须保留官方 issue、当前锁定版本、真机 `.ips` 调用栈和每次实验结果；不得将 IPA 安装成功称为 App 成功，也不得以长期私有 Wry fork 或整仓降级绕过此门槛。

### 14.4 第一阶段实施记录（2026-07-25）

已完成：

- Web 入口切换为文件树 + 中央内容区 + 隐藏设置；Desktop 暂时保留原 `WorkspaceLayout`。
- 绑定项目文件夹只检测，不写入；用户点击“新建记忆空间”后才创建 Wiki 与 `.raw/对话记录/`，且不自动创建首条对话。
- `openProjectResource()` 已统一区分 `conversation / document / media / binary`；普通 Markdown 不进入聊天壳。
- 对话创建、H1 标题、Raw 追加、修订冲突重试和文件树切换已落地；用户消息先落 Raw，合格回复完成后再落 assistant。
- 记忆 Direct 请求只暴露 Skill 元数据与统一 `skill(name)` loader；模型按 description 自主决定是否加载和使用工具，不设强制查询门禁。
- 登录、模型、Skill 仓库和 MCP 进入设置抽屉；图片、视频、音频共用现有 `CreationPanel -> mediaTaskStore`，首次媒体意图时按需加载。
- 桌面宽屏使用左树右内容，手机宽度使用文件树抽屉和全屏中央内容区。
- 记忆模式文件树头部按作用域分两排：第一排为 `Logo + 项目名/切换 + 隐藏文件树`，第二排为新建文件、新建文件夹和刷新；顶部对话选择器旁常驻“新建对话”，切换项目后从 Raw 读取对话列表，没有对话时显示显式创建入口。
- 输入框复用主 App 的 `contenteditable` 纯文本提取和自动增高合同；普通文本文件可从右键菜单“引用到对话”，引用内容只进入下一轮请求，不改写原文件。
- 设置抽屉复用白色、浅色、黑夜和护眼四套主题；记忆 Web 首次启动默认护眼绿色，后续尊重用户选择。
- Web Skill 仓库读取用户已安装 Skill Store；输入框不显示 SkillPicker。`public/skills/` 不进入仓库，但与用户 Skill 合并为模型可发现目录。
- `skill-creator` 的完整 `SKILL.md` 安装块、确认卡和 `createAgent()` 写入已接通；同名 Skill 明示为更新，确认前不写入。

验证证据：

- 对话身份、资源路由、通用 Wiki 和三类媒体合同定向测试 `43/43` 通过。
- `pnpm exec vue-tsc -b`、`pnpm run build:quick` 和 Web 产物审计通过。
- 应用内浏览器已验证：创建项目、五文件 Wiki、创建/切换 Raw 对话、H1 标题、普通文档只读、账号设置、Skill 仓库、MCP 和 `390x844` 手机布局。
- 本轮相关回归 `78/78`、TypeScript、Web quick build 和产物审计通过；应用内浏览器验证五按钮工具栏、文本引用、输入框 `24px -> 120px -> 24px` 自适应、四主题及绿色默认，无新增控制台错误。
- 完整 focused 当前仍有 4 条既有电商/旧 Web 会话源码合同失败，不登记为本阶段通过证据。

### 14.5 第二阶段实施记录：Desktop / Mac 私测 1-3（2026-07-25）

已完成：

- `src/App.vue` 的 Desktop 入口已切换为 `MemoryWorkbench`，移除该入口的 OpenCode session、sidecar 和项目同步初始化；保留现有产品身份、更新检查和本地能力初始化。
- `MemoryWorkbench` 按运行时使用 Web `webProjectId` 或 Desktop `projectDir` 初始化 Wiki 与 Raw，并监听项目切换；Desktop 文件树资源、文本新建和系统打开统一回到记忆工作台资源路由。
- Desktop 对话请求改用现有 `createDesktopProjectToolExecutor`，Wiki 查询、Skill、MCP 和本地项目文件不再误走 Web IndexedDB；设置抽屉复用 Keychain/API Key、Skill、MCP，并补齐 Ollama 本地模型连接入口。
- 已生成本机 Apple Silicon 测试包：`韭菜盒子.app` 与 `韭菜盒子_1.3.0_aarch64.dmg`。`.app` 为 arm64，现有 Developer ID 签名校验通过；未配置 Apple notarization 凭据，因此本次未公证、未上传商店、未触发更新通道。

验证证据：

- 记忆工作台专项契约测试 `12/12` 通过；`pnpm exec vue-tsc -b --pretty false` 通过。
- `pnpm run build:desktop:quick` 通过，Desktop dist 审计通过。
- `pnpm run tauri:build` 通过，产物位于 `src-tauri/target/release/bundle/macos/韭菜盒子.app` 和 `src-tauri/target/release/bundle/dmg/韭菜盒子_1.3.0_aarch64.dmg`；`codesign --verify --deep --strict` 通过。
- 本机启动测试包后出现 macOS “允许访问‘文稿’文件夹”系统权限提示；待用户确认后进行文件树、Raw、Wiki、Skill、MCP、本地模型和媒体人工验收。

2026-07-25 Mac 私测反馈修正：

- 普通文件夹不再因绑定而自动生成 Wiki、Raw 或“新对话”；中央区先显示“新建记忆空间”。
- 记忆空间创建后，中央顶栏始终提供“新建对话”，点击才创建并打开对应 Raw。
- 产品 Logo 与项目名称组合放在文件树项目身份区；中央顶栏保留空白 Tauri 原生窗口拖拽区域。
- Gateway 模型目录保留明确的 `tool_call` 声明；未明确禁用的文本模型按支持工具调用处理，修复新增文本模型被误判为不能查询 Wiki。
- 输入框增加逐轮 `快速 | 记忆`；两者共用同一 Raw，快速不加载 Wiki/MCP 工具，记忆保持强制查询，旧 Raw 默认记忆。
- 粘贴图片统一进入附件区；本轮图片/视频附件经公共 referenceId 合同物化，媒体确认卡与提交任务能自动进入图生图/图生视频。
- 输入框工具栏保留 `快速 | 记忆` 和一个 `Skill：当前值` 入口；Skill 面板向上浮动并与输入框右边缘对齐，不推高输入框，选择或点击外部后关闭。附件与引用使用同一行紧凑标签，不再增加分隔带。
- 中央顶栏左侧把对话选择器与纯文字“新建对话”紧邻成组，右侧放分组文本模型选择器和设置按钮；触发框宽度跟随当前模型名称并设响应式上限，浮层与触发框左对齐。列表按模型家族/厂商分组，保持网关原始顺序和现有 `setModel()` 合同；RunningHub 等内部媒体模型不进入文本模型选择器。
- 文件树项目身份与中央顶栏共用同一高度和主分界线；Logo 做 `2px` 光学下沉并收紧与项目名的间距。新建文件、新建文件夹和刷新位于主分界线下方，搜索框底线只作为文件树内部的弱辅助线，不再用多条同权重横线制造错位。
- 左侧文件树和中央文档预览是两个独立的竖向滚动容器；鼠标滚轮只滚动指针所在区域，内容超长时显示可见滚动条，不让整页或相邻区域代替滚动。
- 验证通过：相关定向回归 `34/34`、完整 focused、TypeScript、Web/Desktop quick build及两端产物审计；浏览器真实验证模式切换、图片粘贴和纯文本粘贴。Apple Silicon `.app` / `.dmg` 重建完成，Developer ID 严格签名校验通过并已启动。

以下是 2026-07-25 当时的未完成记录，已由后续实施与发布记录部分覆盖：

- 第二阶段第 4-5 项（新版 `rh-adapter` / NewAPI RH 3D 配置和真实混元 3D 闭环）尚未执行。
- 尚未完成登录后的真实模型回复、Wiki 确认写入、MCP 调用和付费媒体人工验收；这些不影响本机测试包交付，但不能登记为通过证据。

同日旧清单（保留作为历史，以下状态以 2026-07-26 当前状态为准）：

- 尚未发布到 <https://jiucaihezi.studio>。
- 未使用真实登录账号完成模型回复、Wiki 确认写入和付费媒体闭环。
- Desktop/Mac 第二阶段适配和本机私测尚未实施。
- RH Adapter 新版部署、NewAPI 两个 3D 模型及价格配置尚未完成线上验收。
- Web 预览/正式发布和 Mobile 原生 App 尚未实施。

2026-07-26 当前状态：

- Web 2.0.1 已构建并正式发布到 <https://jiucaihezi.studio>，生产域名已验证加载新资源。
- Apple Silicon Mac 2.0.1 本机测试包已构建、校验并启动；Mobile 原生 App 尚未实施。
- 第四阶段入口改为 4A 文字同步底座，完成 Web ↔ Mac 真机闭环后先完成 4B 多产品构建边界，再进入 4C Mobile。
- Cloudflare 已创建 APAC 独立 D1 `jiucaihezi_sync`，Gateway 配置以 `SYNC_DB` 作为第二绑定；`0001_sync_foundation.sql` 与 `0002_sync_revision_guard.sql` 均已在本地及远端应用，远端三表、基础索引和 revision 唯一门禁已核验。
- 基础 schema 包含 `projects`、`text_files`、`sync_mutations`；服务端递增 `seq` 用作拉取游标，客户端 `mutation_id` 唯一约束负责重试幂等。三表、基础三索引、插入/游标与重复 mutation 拒绝均已实测。
- 为避免两个设备同时读到同一 revision 后静默覆盖，新增 `0002_sync_revision_guard.sql`，由数据库唯一约束 `(project_id, path, revision)` 作为最终并发门禁；D1 batch 冲突整体回滚并返回 `409`。该 migration 已在本地与远端应用并核验。
- Gateway 已上线受 `requireWebUser()` 保护的项目列表/创建、增量拉取、批量推送、项目删除/恢复接口；登录同时签发独立 `sync_session`，不改变普通 NewAPI Key 的模型调用职责。2026-07-26 部署版本为 `72145e62-0944-452d-9fc7-961664ad54c1`，生产 `/health` 返回 `sync.text`，未登录 `/sync/projects` 返回 `401`。
- Web/Mac 共用 `textSyncClient + ProjectTextSync`：本地变更先由 `ProjectFileService` 成功落盘，再进入隐藏的 `.raw/.sync/state.json` 待同步队列。队列保存稳定 `mutation_id`、期望 revision、内容快照和哈希；网络恢复后复用同一 mutation，服务端成功但响应丢失也不会重复递增 revision。
- 项目打开、App 回到前台和用户点击“立即同步”时先拉后推；云端版本冲突时远端保持规范文件，本地版本保存为可见的 `文件名 (冲突 时间).md`。同步失败不阻塞打开或新建本地记忆空间。
- 客户端与服务端双重拒绝 `.sync`、`jc-media`、Skill、MCP/Provider 凭据、`.env*`、系统配置目录和非安全文本扩展名。媒体、API Key、Session 和设置不会进入请求或 D1。
- 自动化已证明 Web -> Mac、Mac -> Web、离线重试、响应丢失幂等、冲突副本和媒体/队列排除；同步/账号/记忆工作台相关回归 `59/59`、完整 focused、TypeScript、Web 正式构建、Desktop 正式构建及两端产物审计全部通过。12 条只适用于旧 `WorkspaceLayout / ChatPanel / 电商工作台 / OpenCode App` 的回滚源码合同已显式标记为跳过，测试内容仍保留，不再阻断当前 `MemoryWorkbench` 产品入口。
- 4A 的 D1、Gateway、客户端增量同步、安全门禁和应用级云项目入口已完成。左上项目中心统一提供本机/云端列表、首次上传、下载到空本地副本和手动重试；已绑定副本继续自动先拉后推。设置页的旧配对控件已移除。Web/Mac 真机验收通过后进入 4B 多产品构建边界，再进入 4C Mobile 原生壳与平台能力审计。
- v2.0.4 真机验收发现启动只恢复普通模型 API Key、没有恢复独立 `sync_session`，造成账号页显示已登录、同步页却判定未登录，上传动作也在请求前被内存空值拦截。同步会话必须和 API Key 一起在启动阶段恢复，并以独立响应式状态驱动项目中心和设置页；普通 API Key 不能冒充同步登录。历史上只保存过 API Key 的用户需要重新登录一次取得 `sync_session`，此后 Web 刷新与 App 重启均自动恢复。
- v2.0.4 真机复验确认同步主体成立：用户在正确的 2.0.4 Mac App 重新登录后，将本地项目上传为云项目；Web 同账号从云项目下载后，`.raw/对话记录/`、完整 `wiki/` 和其他允许的文字文件均出现并可正常使用，内容与 Mac 本地一致。Wiki 从未被过滤；只有含允许文字文件的目录会随文件出现，空目录本身不建立云端记录。
- 真机同时暴露首次同步的两个体验缺口：客户端原先每个 mutation 单独请求，文件多时长时间只显示“同步中”；状态也没有总数、完成数和进度条。现已沿用原 Gateway 批量合同，将上传收敛为每批最多 100 个不同路径，保留同路径 revision 顺序；拉取响应返回当前 cursor 后的不同路径总数，项目中心与设置页统一显示扫描、上传、下载和完成进度。没有新增 D1 表、同步 Store、后台常驻任务或媒体同步。
- 本轮自动验证：205 文件首次上传按 `100 / 100 / 5` 完成；同步/登录/记忆定向回归 `61/61`、Gateway 同步专项 `13/13`、完整 focused `1351` 通过/`12` 跳过/`0` 失败、Rust `397` 通过/`1` 忽略/`0` 失败；TypeScript、Web/Desktop quick build 与两端产物审计通过。
- Desktop 验收必须同时核对运行中进程路径和 `CFBundleShortVersionString`；从旧 DMG 的 `/Volumes/...` 直接启动不会被 `/Applications` 中的新版本替换。本次截图中的旧文件夹选择器来自仍在运行的 `2.0.3` DMG 进程，不是 `2.0.4` 项目中心产物。

### 14.6 导航职责分离决策记录（2026-07-26）

用户实际使用发现：把对话 Raw 和普通资料放在同一文件树中，会迫使用户先找 `.raw/对话记录/` 再回到原对话，造成不必要的导航成本。本次决策修正为：

- 顶部新增轻量对话选择器，负责当前项目内对话的搜索、切换、新建、重命名和删除；它只是 Raw 的实时投影，不建立第二份存储或索引。
- `.raw/对话记录/` 默认从普通资料文件树隐藏；高级用户可通过“显示系统文件”查看原文。
- 文件树只负责 Wiki、Markdown、媒体、画布和其他项目资料；点击资料打开统一临时预览层。
- 预览层不销毁当前对话，顶部固定提供“返回对话”；支持 Esc/关闭返回，并恢复原对话、滚动位置、输入草稿和附件。
- 普通资料预览与对话切换是两条清晰路径；只有顶部对话选择器可以进入聊天，不从普通文档预览层猜测或切换对话。

实施时优先复用现有 `ProjectFileService`、Raw 解析器和中央内容壳，只新增 `ConversationSwitcher` 与 `previewResource` UI 状态。

### 14.7 已验证的产品与工程经验（2026-07-26）

这一阶段最有价值的产出不是某个按钮，而是一组经过真实使用、失败截图和跨端构建验证的工作台原则。后续漫剧、电商和制作工作台可以复用这些原则，但不能机械复制本工作台的业务目录。

#### 经验一：文件是真源，界面只是投影

- Wiki 是项目长期确定性记忆，Raw 是每个对话的完整原始记录；不再增加第二套会话数据库、摘要记忆或隐藏项目状态。
- 对话选择器直接投影 `.raw/对话记录/`，文件树直接投影项目资料。创建、重命名、删除和恢复都回到同一文件合同，避免 UI 状态与磁盘事实分叉。
- 临时附件、远程媒体 URL、工具过程和轮询快照不是项目事实，不应伪装成长期记忆。媒体成功后先写项目文件，再把相对路径写入 Raw。

#### 经验二：同一对话允许不同执行深度

- `快速` 与 `记忆` 不是两个会话或两个产品模式，而是当前一轮的执行策略；两者必须写入同一个 Raw，用户切换时不能丢上下文。
- 快速路径保留完整对话、附件和媒体规划但不提供工具；记忆路径提供 Skill/项目/MCP 工具，由模型按任务自主决定是否调用。
- “快”来自少一次不必要的工具循环，不来自删掉附件、媒体、模型选择或对话历史。

#### 经验三：导航按用户目标分工，不按文件类型堆入口

- 对话选择器只负责回到和管理对话；文件树只负责浏览项目资料；设置抽屉只负责账号、Skill、MCP、主题和模型相关配置。
- 点击文档或媒体进入覆盖式临时预览，而不是销毁对话或把所有文件强塞进聊天渲染器。返回后必须保留滚动、草稿、附件和引用。
- 高频动作必须出现在其对象旁边：新建对话紧邻对话选择器；新建文件、新建文件夹和刷新位于文件树项目身份下方。不能要求用户理解 `.raw` 目录结构才能完成日常操作。

#### 经验四：AI 输出是外部输入，解析器不能只相信理想提示词

- 系统提示规定输出格式，只能提高遵守概率，不能替代解析层兼容和校验。模型可能返回单对象、对象数组或非法内容，应用必须显式区分。
- 同一轮多个独立媒体任务使用一个 `jc-media-plan` 对象数组；工作台逐项物化确认卡，并以“回复轮次 + 计划序号”隔离价格确认、提交、失败和进度。
- 只有成功解析并形成确认卡后才隐藏技术块；解析失败必须保留原文用于排障。禁止 `catch {}` 之后既不显示错误、又把机器 JSON 当普通正文长期留在界面。

#### 经验五：聊天模型看见素材，不代表执行模型收到素材

- 上传、粘贴和项目媒体统一先形成受控 `referenceId`，再通过 `buildExplicitMediaReferences -> materializeMediaPlanReferences -> preparePublicMediaPlan` 进入公共媒体任务合同。
- 不能根据“模型已经描述了图片”推断图生图或图生视频已经成立；确认卡必须展示实际引用素材、执行模型、参数和价格，用户确认后才提交付费任务。
- 图片、视频、音频和 3D 共享模型注册表、确认卡、任务 Store、失败终态和落盘链路；工作台不复制创作面板，也不另造媒体引擎。

#### 经验六：失败必须尽早成为明确终态

- 上游已经明确失败时，adapter 和 App 都必须停止轮询并显示真实原因；不能把明确失败包装成临时 HTTP 故障后无限等待。
- `pending/running` 是可持久化的任务恢复状态，不等于当前进程正在生成。界面的“生成中”和任务数量只能来自运行时确实存在的提交/轮询执行；执行结束必须在 `finally` 清除运行时身份，旧持久化任务不得仅凭状态值占用生成提示。
- 媒体计划解析失败、参考素材失效、模型未注册、价格合同不完整和项目落盘失败都必须在付费前或结果卡上可见。
- 原始链接只作为旧任务或下载失败的降级出口；不能为了界面干净而删除唯一可取回结果的通道，也不能未经基础 HTTPS 检查直接信任任意模型文本中的 URL。

#### 经验七：视觉对称来自职责和基线，不来自强行拉齐所有线

- 左侧项目身份与中央对话顶栏共用同一高度和主分界线；文件动作和搜索属于线下次级层，不要求所有内部辅助线跨越整个页面。
- Logo 与文字采用光学而非纯数学居中；本次 `2px` 下沉是在桌面与窄屏截图中验证后的视觉修正。
- 固定格式控件必须有稳定高度、响应式上限和独立滚动容器。文件树、文档预览、模型菜单和 Skill 菜单各自滚动，不能依赖整页滚动兜底。

#### 经验八：发布证据必须对应真实交付物

- 源码测试、`vite preview`、Cloudflare Preview 和 Production 是四种不同证据；本地页面正常不等于正式域名已更新。
- `4174` 当前是 `vite preview`，源码修改后必须重新构建 `dist/` 才会变化；不能把旧产物误判为新代码的视觉结果。
- 发布前至少完成本次相关定向回归、TypeScript、正式 Web 构建、产物审计、桌面与 `390x844` 浏览器检查。全仓若有已知旧合同失败，必须明确列出且证明与本次差异无关，不能笼统声称全量通过。
- Production 必须使用 `npx wrangler pages deploy dist --branch=main`，保留部署 URL 和上一版回滚点；Git commit、Git push 与 Web 部署分别记录，不能混为一个动作。

#### 经验九：完整文字历史不等于完整媒体上下文

- Raw 中有附件名称，只能证明“曾经上传过”，不能让后续模型重新取得附件本体；附件必须先成为项目文件，再由 Raw 保存稳定定位器。
- 不能每轮重发全部历史图片二进制：这会放大请求、增加成本，并在多图时制造指代歧义。正确做法是向模型提供受控引用清单，只在确认卡和执行边界读取被选中的素材。
- 页面内存中的 Data URL 只服务当前轮预览和模型识图，不是长期记忆。刷新恢复、隔轮复用和卡内换模型必须依赖 `jc-media/uploads/ + Raw projectPath`。

#### 经验十：生成、取回与保存是三个状态，确认卡是可复用参数单

- 上游生成成功、浏览器取回结果、写入项目文件是三个阶段。后两步失败不能把已经付费完成的生成任务改写成“生成失败”；必须保留原始结果，并单独提供“重试保存”。
- `api.jiucaihezi.studio` 返回的 `/v1/videos/{task}/content` 等结果地址可能仍需当前用户 Token。浏览器只对韭菜盒子 API 同源结果下载附加现有认证，不能把 Token 发往第三方 CDN；聊天结果卡直接显示受信执行链返回的安全 HTTPS 地址，项目副本只在用户点击下载、文件树预览或再次引用时读取。
- 媒体确认卡不是一次性按钮，而是当前提示词、参考素材、模型、比例、分辨率和时长的可复用参数单。提交后仍可调整参数并“再次生成”，旧结果不能被新任务覆盖。
- 图片支持一次生成 `1-5` 张；每张都是独立付费任务、独立进度和独立结果。批量提交发生部分失败时，已经提交的任务必须立即显示，并明确提示成功提交数量；视频、音频和 3D 暂不提供批量入口，避免高成本误操作。

#### 经验十一：落盘不是展示缓存，媒体卡不得主动搬运项目文件

- 2026-07-26 两次 Chrome Renderer 崩溃 dump 明确记录 `OOM (CALL_AND_RETRY_LAST)`、V8 `allocation failure` 和当前 `gpt-image-2-vip` 任务。生成请求已返回 HTTP 200、项目文件已保存，崩溃发生在结果展示阶段。
- 根因是每个成功任务卡挂载时都从 OPFS 读取完整媒体，再依次复制为 `ArrayBuffer -> Uint8Array -> Blob -> Object URL` 并解码；批量图片和历史任务卡会并发放大峰值。`URL.revokeObjectURL()` 只能在卸载后释放，不能阻止挂载时的峰值。
- 第一轮止血只取消了任务卡自动读取 OPFS，仍让完整历史和每个视频/音频播放器长期挂载；`preload="metadata"` 不能释放媒体元素、解码上下文和 GPU 资源，因此不是最终架构。

#### 经验十一补充：任务、资产、轻量卡片、按需预览必须分层

横向依据：

- OpenCode `message-timeline.tsx` 使用 `@tanstack/solid-virtual`；LobeHub `VirtualizedList.tsx` 使用 `virtua`。两者都只挂载可视消息，并为生成中或用户正在选择的消息保留例外。
- Open WebUI 默认只挂载最近 8 条消息，向上滚动再扩展，并用 `content-visibility` 降低非可视布局成本。
- ComfyUI 的任务资产列表使用 `@tanstack/vue-virtual`；`LazyImage.vue` 通过 `IntersectionObserver` 进入视口才取得媒体，离开后释放 Blob URL；视频播放器只存在于结果查看组件。
- InvokeAI Gallery 使用 `react-virtuoso` 的 windowed list，只为当前可视范围批量取得完整 DTO，原图进入独立 Viewer。
- 本项目主 App 已采用同一原则：文件树虚拟化；视频在画布中只保存项目路径、静态首帧和播放按钮；Desktop 首帧由后台 `ffmpeg` 单通道生成；点击后才建立唯一预览播放器。

公共媒体合同：

```text
媒体任务：状态、模型、提示词、参数、进度、错误
  -> 媒体资产：assetId、项目路径、MIME、缩略图/封面、原始结果地址
  -> 轻量载体：对话卡、文件树、画布只消费元数据和缩略图
  -> 按需查看：用户点击后读取本地项目文件，并建立唯一预览实例
```

- Raw 只保存稳定任务/资产身份和项目相对路径，不保存媒体二进制、Data URL、Blob URL 或播放器状态。
- 记忆对话复用项目现有 `@tanstack/vue-virtual` 动态高度时间线，只挂载可视区及 overscan；运行中的轮询属于 Store，不依赖任务卡是否挂载。
- 图片卡可使用安全 HTTPS 缩略展示，但必须 `loading="lazy" + decoding="async"`；视频、音频和 3D 卡只展示静态类型、模型、保存状态和操作，不在时间线建立播放器或渲染器。
- 点击已落项目的结果统一进入现有中央资源预览；任何时刻最多存在一个视频、音频或 3D 预览实例。关闭预览、切换资源或切换对话必须停止播放并释放本地 Object URL。
- 项目文件是设备内持久化真源，远程结果地址只负责刚完成时的轻量图片展示和保存失败兜底；下载、打开和再次引用才读取项目二进制。
- Desktop、Web、Mobile 共享上述身份和生命周期合同，但缩略图实现允许按平台不同：Desktop 后台 `ffmpeg`，Web 无缩略图时显示静态视频封面，Mobile 后续使用原生媒体框架。

压力验收：同一对话至少覆盖 100 轮文字、50 张图片、20 个视频；未打开预览时 DOM 中不得存在 `video`/`audio`，打开时同类播放器最多 1 个；连续滚动、切换对话和关闭预览后不再持有离屏播放器、Blob URL 或已完成任务轮询。Web Chrome、Mac App 和手机真机必须共用这组门禁。

#### 经验十二：项目二进制重读必须恢复真实 MIME

- 2026-07-26 Mac 2.0.1 再次提交历史参考图时，RH 明确返回 `Unsupported file type: application/octet-stream`；这不是渠道集体失效，而是 Desktop 文件读取只返回字节和大小，计划重建的项目定位器又没有 MIME。
- Raw 虽然正确保存了原附件 `image/jpeg`，媒体计划只持有项目路径；付费提交重新读取后若不从明确元数据或受控扩展名恢复 MIME，数据 URL 就会退化为 `application/octet-stream`。
- 正确边界在共享 `ProjectFileActions.readMediaDataUrl()`：存储层 MIME 优先，其次资源 MIME，最后才按受控媒体扩展名推断。不得在 Seedance、RH adapter 或单个模型分支里打补丁。

#### 经验十三：媒体确认卡确认的是用户意志，不是模型改写

- 用户明确写出提示词或动作描述时，媒体计划必须原样使用；只有用户明确要求优化，或没有提供可执行描述时，模型才可以补全。专业化 Skill 是可选增强，不能默认覆盖用户原话。
- 付费提交前的提示词必须和模型、比例、分辨率、时长一样可编辑；确认卡里的当前值才是最终提交真源。
- 模型 ID 和路由不能为了视觉简洁被隐藏成无法区分的同名项。同一展示名来自不同执行来源时，下拉菜单必须补充“直连 / RunningHub”，但不新增渠道分组或改变路由。

### 14.8 多产品构建边界实施记录（2026-07-27）

已完成：

- 默认 `src/App.vue` 继续承载记忆工作台；新增 `src/StudioApp.vue` 恢复主 Studio 的 `WorkspaceLayout + OpenCode` 启动链路。
- Vite 使用构建期 alias 静态选择入口：默认记忆目标转换 431 个模块，Studio 目标转换 3062 个模块；没有运行时产品开关或把另一产品壳打成隐藏 chunk。
- 默认 `tauri.conf.json` 保留已发布记忆 App 的 `com.jiucaihezi.desktop` 身份，但 `externalBin` 为空；`tauri.studio.conf.json` 使用 `com.jiucaihezi.studio.desktop`，独立声明 `binaries/opencode`、Deep Link 和更新通道。
- 正式 Mac/Windows CI 不再下载或复制 OpenCode，并把原“sidecar 必须存在”冒烟测试反转为“记忆包出现任何 opencode 文件即失败”。Studio 的 runtime 下载器、解析器和版本一致性测试继续保留。
- Apple Silicon 实际构建验证：记忆 `.app` 为 14MB，包内没有任何 `opencode`，签名严格校验通过；Studio `.app` 为 146MB，明确包含 131MB 的 OpenCode sidecar。两者 Bundle ID、前端入口和原生资源边界均不同。
- 完整 focused 与 Rust 门禁通过；记忆和 Studio 前端正式构建、Desktop dist 审计均通过。Rust 当前仍共用同一原生命令层；本阶段不为未证明的额外体积收益引入大规模 feature gate，Mobile 平台兼容审计在 4C 处理。

结论：后续电商和漫剧工作台只需增加各自产品入口与 Tauri 覆盖配置，继续复用通用底座，不复制仓库，也不把其他工作台和 OpenCode 一起打包。

### 14.9 记忆工作台与创作面板融合实施记录（2026-07-27）

已完成：

- 记忆工作台首次收到 `MediaPlan` 后才异步加载并展开现有 `CreationPanel`；普通对话启动不加载创作面板与画布。
- 媒体计划的提示词、模型、比例、分辨率、时长和项目素材引用装入创作面板；只有用户在面板内点击生成才提交付费任务。
- 进度、取消与历史仍由唯一 `mediaTaskStore` 和创作面板承担；对话时间线不再渲染完整 `MediaPlanCard`，只在任务结束后回写成功或失败回执。
- 创作面板直接导入与输入框 `@` 引用都先落当前项目，再以项目资源进入画布和本次参考；Web 与 Desktop 不保存第二份素材。
- 生成结果默认只进入项目 `jc-media` 和任务历史，不再自动插入画布；用户点击“放到画布”时，图片按自然宽高比建立节点。

验证证据：

- 创作面板与记忆工作台定向合同 `57/57` 通过，1 条旧电商回滚合同保持跳过。
- 完整 focused `1351/1351` 通过，12 条历史用例保持跳过；Tauri `397/397` 通过，1 条保持忽略。
- TypeScript、Web 正式构建、Desktop quick build 与两端产物审计通过。
- `1280x720` 真实浏览器验证普通启动时无 `CreationPanel` 资源、无横向溢出、无控制台错误。

### 14.10 创作面板布局与项目资源交互收尾实施记录（2026-07-27）

已完成：

- 记忆工作台接回主 App 的可调分栏：创作面板默认约占可用宽度三分之一、最小 520px，对话最小 420px，拖拽宽度写入本机偏好；空间不足时优先收起文件树。
- 三栏工具栏统一为 52px，Desktop 保留独立 28px 原生标题区；创作面板一级保留定位画布、新建画布、历史、提示词参考、专注创作和收起面板，删除与文件树重复的“新建项目文档”。
- 历史预览通过记忆工作台已有的项目资源预览入口在对话区打开；历史“放到画布”、文件树右键“加入画布”和内部项目媒体拖入画布统一消费项目资源引用。
- 对话区恢复主 App 的可拖动竖向滚动条和粘性跟随：停留底部时跟随流式输出，用户向上阅读时不抢滚。

验证证据：

- 新增和更新的三组定向合同 `90/90` 通过，1 条旧电商合同保持跳过；完整 focused `1354/1354` 通过，12 条历史用例保持跳过。
- Tauri `397/397` 通过，1 条保持忽略；TypeScript、Web 正式构建、Desktop quick build及两端产物审计全部通过。
- `1600x1000` 真实浏览器验收三栏宽度为 `280 / 800 / 520`，三栏工具栏均为 52px且无横向溢出；创作面板拖到 615px 后刷新可恢复该宽度。
- 真实浏览器已验证文件树右键“加入画布”、专注创作和退出专注；内部项目媒体拖入画布由数据合同和组件测试覆盖，仍保留 Desktop/Web 人工拖拽验收项。

### 14.11 第四阶段 4C iPhone 原生壳启动记录（2026-07-27）

已完成：

- 从 `v2.0.9` 稳定提交建立独立 `codex/iphone-app` 分支；Desktop 正式发布与 Mobile 开发互不混入。
- 使用 Tauri v2 官方 `ios init` 生成本机 Xcode 工程；`src-tauri/gen/apple` 继续遵守官方可再生目录规则，不提交生成物或复制第二套业务源码。
- 安装 `aarch64-apple-ios`、`aarch64-apple-ios-sim` 和 `x86_64-apple-ios` Rust targets，并完成 Xcode 首次启动组件注册。
- 首次交叉编译定位到 `rfd` 文件选择器和 `trash` 回收站库被无条件编入 Mobile。现已将两者限制为 Desktop target；移动端对应命令明确要求后续接入系统附件、系统分享和移动删除确认，不伪装桌面文件夹或废纸篓能力。
- `cargo check --target aarch64-apple-ios` 与 `cargo check --target aarch64-apple-ios-sim` 均通过，证明共享 Rust 壳已跨过首个 iPhone 编译边界。
- Xcode iOS 26.4.1 Simulator Platform 已安装；`tauri ios build --debug --target aarch64-sim --no-sign --ci` 完整通过，生成的 `.app` 已安装到 iPhone 17 Pro 模拟器并显示产品图标。
- 模拟器启动在业务代码执行前崩于 `wry::platform_webview_version -> NSBundle::bundleWithIdentifier -> CFRelease`。调用栈与 Tauri 官方未关闭的 [#14675](https://github.com/tauri-apps/tauri/issues/14675) 完全一致，属于当前 Tauri/Wry 在新版 iOS 运行时的上游问题；不通过整体降级 Tauri 或复制本地 fork 掩盖。
- Mobile 身份已确定并写入 `tauri.ios.conf.json`：Bundle ID 为 `com.jiucaihezi.mobile`，Apple Team ID 为 `RXD4L9387J`；Apple Development 证书和开发 Provisioning Profile 已生成。
- 真实 iPhone 13 Pro Max 已连接并受信任。官方 `tauri ios dev` 完成 `aarch64-apple-ios` 编译、Xcode 签名、IPA 导出、真机安装和启动请求，设备报告确认 Bundle ID 与 Team ID 正确。
- Xcode GUI 构建脚本最初因环境中找不到 `pnpm` 失败；本机只在已忽略的可再生成 `src-tauri/gen/apple/project.yml` 中补充 PATH 后重新生成工程，不把机器路径或生成物提交为产品实现。
- 解锁后的真机启动仍在业务代码执行前崩于同一 `wry::wkwebview::platform_webview_version -> NSBundle::bundleWithIdentifier -> CFRelease`，系统报告为 `CFRelease() called with NULL` / `SIGTRAP`。真机证据确认这不是 Simulator 特例，也不是登录、同步或 Vue 页面代码故障，而是同一个 Tauri [#14675](https://github.com/tauri-apps/tauri/issues/14675) 上游运行时阻断。

仍待外部条件：

- Simulator 与真实 iPhone 运行均受 Tauri 官方 #14675 阻塞；上游修复或确认兼容版本后，必须重新完成“启动进入首页并保持存活”的运行验收。当前只能将编译、签名、导出、安装和启动请求记为通过，不能宣称 iPhone 壳已经可运行。
- 运行时阻断解除后，才能继续验收首页、登录、云项目和后续 4C 业务闭环；TestFlight 尚未开始。

### 14.12 4C-6 官方版本与真机复核记录（2026-07-27）

- 已直接复核 [Tauri #14675](https://github.com/tauri-apps/tauri/issues/14675)：issue 仍为 open；2026-03-13 的后续报告仍确认 `wry::webview_version()` 在 iOS 崩溃。评论中“`tauri-runtime-wry = 2.6.0` / `wry = 0.37.0`”仅为社区降级 workaround，不是官方已发布修复，且会整体倒退稳定依赖，未采用。
- 当前分支实际解析为 `tauri 2.11.2`、`tauri-runtime-wry 2.11.2`、`wry 0.55.1`。截至本次核查，官方最新 `tauri 2.11.5`、`tauri-runtime-wry 2.11.4` 仍依赖 `wry ^0.55.0`；没有新的已发布 Wry 兼容版本可替换验证。
- Wry `dev` 分支的 `platform_webview_version()` 仍在 iOS/macOS 共用路径调用 `NSBundle::bundleWithIdentifier("com.apple.WebKit")`，没有针对 #14675 的已发布或主干修复。2025-08 的 Wry #1609 仅把 macOS 的空 bundle unwrap 改为错误返回，不能阻止 iOS 上系统 `CFRelease(NULL)` 在该消息发送中终止进程。
- 真机 13 Pro Max 已用 `devicectl` 直接导出最新报告 `韭菜盒子-2026-07-27-174909.ips`：`bundleID=com.jiucaihezi.mobile`，时间 `2026-07-27 17:49:09 +0800`，`EXC_BREAKPOINT` 调用栈为 `NSBundle::bundleWithIdentifier -> wry::wkwebview::platform_webview_version -> tauri_runtime_wry::Wry::init`。这发生在 `jiucaihezi_app_lib::run` 的 Tauri runtime 初始化内，首页和 Vue 业务代码尚未执行。
- 实验记录：基线 `tauri 2.11.2 / tauri-runtime-wry 2.11.2 / wry 0.55.1` 已构建、签名、安装；真机启动失败并生成上述 `.ips`。候选 `tauri 2.11.5 / tauri-runtime-wry 2.11.4 / wry 0.55.1` 无法构成兼容性实验，因为 Wry 版本和故障调用点不变；尝试解析该候选版本时 crates.io 索引网络卡住，停止后确认 `Cargo.lock` 未变，未构建、未安装、未启动。

结论：4C-6 受上游 Tauri/Wry #14675 阻断，未完成。下一条可执行路线是等待 Tauri/Wry 发布明确包含 iOS `platform_webview_version` 修复的稳定版本；届时在本分支先锁定该官方版本，记录 Cargo 解析和 iOS 构建，再在本 iPhone 13 Pro Max 安装并验证冷启动进入首页、连续 60 秒存活且无新 `.ips`。在此之前不启动 4C-7 及后续业务项，不进入 TestFlight。

## 15. 验收标准

### 15.1 导航与资源

1. 应用级项目入口统一显示本机项目和云端项目；进入工作台后不重复显示项目选择器，也不显示四模式入口，顶部只有一个基于 Raw 的对话选择器。
2. 选择器可搜索、切换、新建、重命名和删除当前项目对话，且不创建第二份对话存储。
3. 只有选择有效对话 Raw 才出现聊天记录和输入框。
4. 普通 Markdown/文本在临时预览层按文档显示，不出现聊天输入框。
5. 图片、视频、音频分别可预览或播放。
6. 二进制、超大文本和 `.jccanvas` 不进入聊天或可写文档，只有安全动作。
7. 选择器显示 H1 标题，但真实文件名和 ID 不变。
8. 刷新后只根据项目文件恢复，不依赖第二份对话列表数据库。
9. 文件树第一排显示 Logo、当前项目名/切换入口和隐藏按钮，第二排只显示新建文件、新建文件夹和刷新；“新建对话”紧邻顶部对话选择器。
10. 点击文件树中的资料打开统一预览层；返回、Esc 或关闭后恢复原对话、滚动位置、草稿和附件。

### 15.2 对话与 Wiki

1. 新建对话立即创建带有效标记的唯一 Raw。
2. 用户消息先写 Raw，再发模型；完整回复后写 assistant Raw。
3. 模型看到当前对话和 Skill 目录后，按 description 自主决定是否调用 `jc-cha-wiki` 或其他 Skill；App 不预查关键词、不强制调用。
4. 模型只有实际调用并成功取得工具结果后，才可声称查询或修改了项目；普通回答无需工具门禁。
5. 普通聊天不写 Wiki；用户确认后可写合理路径并追溯到 Raw turn。
6. 清除 UI 状态后，全部对话仍可从 Raw 恢复。
7. 普通文本文件可引用到当前对话；引用只进入本轮模型请求，原文件保持不变。
8. 绑定普通文件夹不自动写入；点击“新建记忆空间”后才建立 Wiki/Raw，点击“新建对话”后才建立对话 Raw。

### 15.3 设置、Skill 与 MCP

1. 账号/齿轮按钮可打开登录、Provider、模型、Skill 仓库、MCP 和通用设置。
2. 设置不是常驻第三栏，关闭后回到原资源。
3. Skill 仓库和输入框 `@` 菜单只显示用户已安装 Skill；新增、编辑或删除后自动发现目录与菜单立即同步。`@` 同时可引用当前项目文件和媒体。
4. `public/skills/` 中的产品内置 Skill 不在仓库出现，但与用户 Skill 一样由模型按 description 自主调用。
5. 用户明确要求安装后才出现安装确认卡；点击“继续修改”不写入，点击“安装到我的 Skill”才写入并同步两处列表。
6. 只有 `enabled + connected` 的 MCP 工具进入模型候选池。
7. Web 不显示不可执行的本地 `stdio`；Desktop 可使用现有本地与远程 MCP。
8. 凭据不进入 Raw、Wiki、日志或模型上下文。
9. 设置提供白色、浅色、黑夜和护眼四主题；记忆 Web 首次启动默认护眼绿色。
10. 输入框只显示一个 Skill 入口；列表向上浮动、不改变输入框尺寸、不越出中央内容区，选择或点击外部后关闭。
11. “新建对话”不显示装饰图标；文本模型列表按 Claude、GPT、Gemini、Grok、DeepSeek、通义、智谱、豆包、本地模型和其他分组，不显示 RunningHub 媒体模型；触发框随当前模型名称自适应，展开浮层与其左对齐。
12. 文件树和文档预览内容超出可见区时，两者均可用鼠标滚轮独立上下滚动，并显示可拖动的竖向滚动条。

### 15.4 媒体

1. 不选择媒体 Skill 也能自然语言生成图片、视频和音频。
2. 三种媒体使用同一公共计划、现有 `CreationPanel`、`mediaTaskStore` 和落盘合同。
3. 模型返回媒体计划后右侧创作面板自动展开并装载草稿；未在面板明确点击生成不提交付费任务。
4. 进度、取消和任务历史只在创作面板显示；对话只显示最终成功或失败回执。
5. 结果分别写入 `jc-media/images`、`videos`、`audios`，Raw 只记项目相对路径。
6. 成功卡可一键下载副本，也可定位并打开文件树中的项目文件；界面不暴露远程 URL。
7. 开始生成与生成中的强调色随当前主题变化，不出现独立紫色。
8. `CreationPanel` 与画布只在首次媒体意图后按需加载并持续挂载；不得新增第二套任务引擎。
9. 当前媒体草稿可改模型、比例、分辨率、时长、提示词和参考素材后再次生成；历史任务不被草稿修改覆盖。
10. 图片草稿可选择一次生成 `1-5` 张，每张形成独立任务；部分提交失败不隐藏已提交任务。
11. 上传或粘贴的图片、视频立即保存到 `jc-media/uploads/`，Raw 只保存项目相对路径和附件元数据，不保存二进制副本。
12. 隔多轮后可用明确指代复用历史上传素材；只有一个候选时直接进入带缩略图的确认卡，多个歧义候选时不得静默选错。
13. 刷新页面、重新打开对话或在原确认卡切换模型和参数后，图生图/图生视频仍使用原项目素材。
11. 上游生成成功但项目保存失败时，结果仍按成功展示，并提供原始结果与“重试保存”；不得显示为“生成失败”。
12. 韭菜盒子 API 的受保护结果下载携带当前 Token，第三方媒体域名绝不携带该 Token；播放优先使用项目内已保存副本。
13. 对话、创作面板、画布和文件树对同一素材只保存项目资源引用；创作面板直接导入或 `@` 选择媒体后，该素材写项目一次并在画布中选中。
14. 生成成功不自动向画布追加节点；用户明确“放到画布”后按结果真实宽高比显示，不得沿用参考图边界拉伸结果。

### 15.5 发布

1. Mac 私测必须先于 Web 正式发布，且使用本机测试构建，不触发正式更新通道。
2. RH 3D 真实验收前，服务器必须完成新版 `rh-adapter` 重建，以及 NewAPI 的 `rh-3d-text` / `rh-3d-image` 模型和价格配置。
3. Web 正式发布前，必须先完成同一 Pages 项目的预览部署和真实浏览器验收。
4. Web 预览验收通过后，正式发布到 <https://jiucaihezi.studio>；旧部署保留用于回滚。
5. Mobile 在 Web 正式发布稳定后再建立移动发布配置。
6. 当前阶段不存在 `chat.jiucaihezi.studio`、`com.jiucaihezi.chat`、`jiucaihezi-chat://`、`chat-v*` 或 `/updates/chat/`。
7. 现有主 App 源码、签名身份和历史发布可用于回退。

### 15.6 跨设备文字同步与 Mobile

1. 同一账号在 Web、Mac 和 Mobile 看到相同的云项目列表；不同账号即使猜中 project ID 也只能得到统一的不可见响应。
2. 本地项目可通过“上传到云端”建立云项目；另一设备点击云项目并“下载到本机”后创建新的本地副本、保存映射并直接打开，不要求用户先创建同名项目。
3. 首次下载后只做增量拉取/推送；重复同步不得删除整个本地项目或用云端快照静默覆盖未上传变化。
4. `.raw/对话记录/`、Wiki 和允许的普通文本可双向增量同步；媒体二进制、Skill、MCP、Provider、API Key、Session 和设置不会进入请求或 D1。
5. 手机新增一个用户 turn 和完整 assistant turn 后，Mac 拉取即可继续同一对话；Mac 写入 Wiki 后，手机拉取能用于下一轮记忆查询。
6. 断网发送和 Wiki 写入先在本地成功并显示“待同步”；恢复网络后按同一 mutation id 上传，不生成重复 turn 或重复文件。
7. 两台设备基于同一 revision 修改同一文件时，后一提交收到 409；Raw 只有稳定 turn 可无歧义合并时才自动合并，其他文件保留冲突副本。
8. 删除本地副本不删除云项目；云端删除同步到离线设备后不会被旧副本复活，30 天 tombstone 期间可以恢复。
9. 新设备打开含 `jc-media/...` 引用的对话时，文字和计划正常显示，缺失媒体明确标注“仅保存在原设备”，无自动远程下载和崩溃。
10. D1 中没有绝对路径、二进制/Base64、临时媒体 URL、系统提示词、隐藏推理或任何凭据。
11. Mobile 使用 App 管理目录和同一 `ProjectFileService` 语义；不因平台不同复制 Raw 解析、Wiki 工具或媒体计划实现。
12. iPhone 真实设备完成登录、前后台恢复、断网重试、文字双向同步、对话、Wiki、附件和本地媒体闭环后，才开始 Android 发布矩阵。
13. 首次上传 205 个不同路径时必须形成 `100 / 100 / 5` 三批请求；同步界面依次显示扫描、上传或下载的完成数/总数，结束后显示“文字已同步”，并明确媒体与空目录不参与同步。

## 16. 成功路径

```text
打开 https://jiucaihezi.studio
  -> 创建或导入项目
  -> 若项目尚未初始化，点击“新建记忆空间”（仅此时写入 Wiki/Raw 骨架）
  -> 文件树出现 wiki 和已有资料；对话选择器读取 .raw/对话记录/
  -> 点击顶部“新建对话”并开始聊天
  -> 用户消息写入当前 Raw
  -> 模型读取当前对话并自主查询 Wiki
  -> 模型回复写回同一个 Raw
  -> 从顶部选择器切换另一个对话，中央区恢复并继续该对话
  -> 点击 Wiki 文档，打开临时预览层；返回后继续原对话
  -> 点击图片/视频/音频，打开临时预览层或播放器；返回后继续原对话
  -> 从设置登录、选模型、管理 Skill 和 MCP
  -> 在对话内确认并生成媒体，结果落当前项目
  -> 用户确认后把结论写入 Wiki
  -> 点击“上传到云端”，建立 cloud project_id 并上传允许的文字文件
  -> Mac/Mobile 登录同一账号，在项目入口看到该云项目
  -> 点击“下载到本机”，创建本地副本并直接打开同一 Raw/Wiki
  -> 后续本地 Raw/Wiki 变更进入待同步队列，“立即同步”先拉后推
  -> 手机本地媒体不上传；Mac 只显示“媒体仅保存在原设备”
  -> Mac 再次打开后增量拉取手机新增文字并继续
  -> 关闭重开，仍先从本地项目 Raw、Wiki 和媒体恢复
```

这条路径成立，就说明第一个工作台完成跨设备闭环。第四阶段必须先让 Web 与 Mac 通过同一同步合同，再接 Mobile；不提前创建三套业务代码，不让手机成为第二个数据孤岛。
