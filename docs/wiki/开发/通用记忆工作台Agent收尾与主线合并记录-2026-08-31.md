# 通用记忆工作台 Agent 收尾与主线合并记录

> 日期：2026-08-31
> 状态：收尾记录；合并前备份

## 本轮范围

- 产品保持单一 Agent。Wiki、Skill、文件读写、媒体、3D、Terminal 和 MCP 都是同一 Agent 可授权调用的能力，不创建并列 Agent。
- WikiAgent 规范冻结为当前 Wiki 读取、Skill-first、工具白名单、Observation/Receipt、取消和一次确定性写入的实施合同。
- `0829-rhapp-prompt-141` 已合并到本地 `main`，保留 RH 提示词框的最小修复。
- `codex/0830-skill-first-agent` 包含最近对话恢复、消息底部操作、输入区三行高度、`@图文`/`@影音`、历史图片缩略图、MLX 启动连接和 SA1-SA4 实现，合并前先提交其工作区改动。

## 证据与边界

- 当前 `0829-WikiAgent` 工作区的 WikiAgent 代码、规范和测试改动待提交；`main` 尚未包含这些未提交改动。
- Skill-first 分支已有提交 `0a3e2ad5` 至 `f72af36a`，另有未提交界面与对话收尾改动；合并后需重新执行回归。
- 自动验证以 focused 测试、`vue-tsc -b`、Rust 检查、构建审计和 `git diff --check` 为准；真实模型、安装包、跨平台和生产发布验收不因自动测试通过而自动宣称完成。
- 本记录不保存用户对话正文、媒体内容、API Key、模型响应或临时截图。

## 收尾顺序

1. 已提交 `0829-WikiAgent` 的规范、路由、工具白名单、取消和测试改动，提交 `49b515fc`。
2. 已提交 `codex/0830-skill-first-agent` 的界面、MLX 和对话收尾改动，提交 `bcaa9163`。
3. 已将两个提交集合合并到 `main`，合并提交为 `1c83690e`；重叠文件保留 WikiAgent 合同并接入 Skill-first Tool Search。
4. 合并后验证通过：Node focused `1243/1243`、Rust `403 passed / 1 ignored`、`vue-tsc -b`、`git diff --check`。
5. 已删除已合并临时分支；历史文档和未完成真实模型、安装包、跨平台、生产验收边界继续保留。
