# 2026-07-20 工作区边界与发布知识专项巡检

> 范围：GitHub 推送、Web/桌面发布、工作区清理，以及它们关联的现行 Wiki。
> 方法：`jc-jian-wiki` 机械扫描、现行事实页交叉阅读、Git 追踪状态核对。
> 复巡结论：❌ 必修 0 / 🔻 重要 0 / ⚠️ 建议 0。

## 修正记录（2026-07-20）

### ✅ 电商工作台 SDD 的 Raw 账本描述已更新

- 证据：[[开发/电商工作台SDD]] 的架构图、约第 198 和 311 行称创模式会写入 `.raw/sessions/jcses_*.jsonl`。
- 冲突来源：[[开发/创模式Raw账本与对话Wiki移除SDD]]、[[hot]] 与当前生产源码审计均确认该写入链路已于 2026-07-20 删除；用户已有 `.raw` 只作为主动放入的原始资料保留。
- 影响：后续开发者若按电商 SDD 实现或验收，可能错误恢复会话账本写入，也会误判个人会话是否应进入 Git。
- 已修：[[开发/电商工作台SDD]] 全部 7 处现行 Raw 账本描述改为创模式 UI 会话与项目媒体结果链路；历史账本方案保留在已标注“历史实现”的 [[开发/创模式记忆底座SDD]]，删除依据保留在 [[开发/创模式Raw账本与对话Wiki移除SDD]]。

### ✅ 工作区与发布学习结论已进入长期入口

- 证据：[[学习/GitHub推送与发布边界-2026-07-20]] 已存在且登记于 [[来源索引]]、[[log]]，但 [[CLAUDE]] 的“学习”入口只列出 AI 编程生存手册。
- 影响：新开发会话较难发现 Git、发布和工作区边界这份直接相关的学习材料。
- 已修：[[CLAUDE]] 的索引新增“工作区与发布”，指向 [[学习/GitHub推送与发布边界-2026-07-20]]。

### ✅ 巡检器已降低代码示例与特殊文件名的误报

- `scan_vault.py --vault docs --mode wiki` 报告 43 条“断链”和 221 个“孤儿笔记”。复核后，告警主要来自 `归档/` 内代码示例的 `[[...]]` 字符串、历史项目示例和特殊文件名；它们不是活跃 Wiki 导航断链。
- 二次去除代码块并按实际路径、相对路径和唯一标题解析后：285 个 Markdown 页面中，未发现活跃现行页面的确定性断链；剩余 20 条均在归档示例或扫描器无法表达的特殊文件名中。
- 已修：`scan_vault.py` 忽略 fenced/inline code，并纳入 `*.md (标识)` 页面；新增契约测试。复跑后断链由 43 降为 16，剩余均在 `归档/` 的历史示例中，未对其批量修改。

## 已核对且一致

- Git push 只上传已暂存并提交的文件；Web 仅部署通过构建审计的 `dist/`；桌面发布由版本 tag 的 CI 构建，见 [[学习/GitHub推送与发布边界-2026-07-20]]、[[架构/产品架构]]、[[开发/v1.3.0发布失败修复SDD]]。
- 当前 `.raw/sessions/` 有 2 个已追踪文件、`.git.backup/` 有 7,356 个、`.superpowers/` 有 20 个；这些是 Git 清理事项，不会因构建自动进入 Web 或桌面包。
- 当前旧 `dist/` 的 `.DS_Store` 会导致 `pnpm run audit:web-dist` 失败；不得将它直接作为发布产物。
