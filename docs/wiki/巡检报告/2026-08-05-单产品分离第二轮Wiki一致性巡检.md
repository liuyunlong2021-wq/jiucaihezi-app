# 单产品分离第二轮 Wiki 一致性巡检

> 日期：2026-08-05
> 范围：当前产品入口、开发指令、工作区配置、随包 Wiki Skill、来源索引与旧产品架构页
> 结论：已修正并通过自动复检

## 问题

1. README、AGENTS 和 VS Code 工作区仍把仓库描述为 Studio/OpenCode 产品，并挂载已迁出的本地工程。
2. `jc-everything-wiki` 与 `jc-raw-wiki` 仍把 Studio 会话和 OpenCode `ses_*` 生成为来源索引真源，与当前 Raw Markdown 合同冲突。
3. `架构/翻译方针.md`、`架构/对照表.md`、旧模式 Canvas 和旧存储 SDD 仍位于现行分类，可能被查询为当前设计。
4. 当前记忆 SDD、生存手册、来源索引和发布边界页仍有少量旧产品措辞、失效路径或失效链接。
5. 产品架构把 `com.jiucaihezi.mobile` 泛化为全部 Mobile，未说明 Android 当前无稳定独立身份。
6. Gateway 根路径测试仍期待旧 Studio landing，同时代码残留无调用者的宽泛 `/api/*` 代理，与登录/同步专用边界及既有禁入测试冲突。
7. 深层巡检发现随包 Wiki Skill 仍写“Studio 原生”，现行文件系统页仍引用已删除编辑器，旧产品草案仍在开发目录；追加式日志的历史断链也被误算为现行错误。
8. 图标生成器漏扫 `:name` 动态字面量，播放按钮会缺少 `play-arrow`；Web/Desktop 产物会携带 Python 缓存，且 `lowlight` 已无调用者。
9. 分离 SDD、新手指南、推荐指令和一次性迁移配置仍把 20 个个人写作、视觉、旁白 Skill 当作随包产品能力，与用户最终确认的 7 项产品 allowlist 冲突。
10. 旧客服 Agent 草案仍设计静默上传用户问答，个人 `jc_media.py` Skill 方案仍位于现行开发目录，两者都可能被误读为当前合同。

## 修正

- README、AGENTS 与工作区配置收口为通用记忆工作台；工作区只挂载当前仓库。
- 随包 Wiki Skill 的对话来源统一为当前项目 `.raw/对话记录/*.md`，明确 Markdown 是唯一持久化真源；保留 VS Code Chat 和外部文件只读来源。
- 四份旧架构/存储资料完整移动到 [[归档/单产品分离前/README]]，现行导航改指记忆工作台架构和文件系统。
- 修正当前审批 SDD、Wiki Skill 规范、生存手册、来源索引与发布边界链接；历史案例保留并明确不代表当前架构。
- 产品架构和热缓存明确 `com.jiucaihezi.mobile` 仅为 iOS 现有身份；Android 继续暂停。
- Gateway 根路径测试对齐现行账号登录跳转；删除无生产调用者的旧 `/api/*` 同源代理，登录、同步、MCP OAuth 和静态资源路由保持不变。
- 删除最后一份已失效 Tiptap smoke 试验说明，并把整个目录加入分离门禁；对应正式历史文档继续保留在 `docs/wiki/归档/tdd/`。
- 五个随包 Wiki Skill 统一使用“App 原生”合同；现行文件系统入口改指 `MemoryWorkbench`、`ProjectFileTree` 和项目资源服务。
- 旧创模式 MCP 草案与旧编辑区 SDD 移入 [[归档/单产品分离前/README]]；当前记忆 SDD 移除失效旧产品链接和“主 Studio”措辞。
- Wiki 巡检把 append-only `log.md` 归入历史卫生，不再把不可改写的旧链接当作现行错误；当前断链已清零。
- 图标生成器覆盖动态 `JcIcon :name`；Web/Desktop prune 与 audit 递归清理并禁止 `__pycache__/.pyc`；删除无调用的 `lowlight` 及孤立旧测试 helper 说明。
- 以 frozen lockfile 完成离线安装，物理依赖与 lockfile 一致。
- 20 个个人 Skill 先完整迁入 `/Users/by3/Documents/jiucaihezi-personal-skills` 并提交，再从 App 删除；随包索引、新手指南、推荐指令、Skill 创建模板和旧迁移配置收口到 7 个产品 Skill。历史 SDD 只保留为历史证据。
- 旧客服 Agent 与个人媒体 Skill 方案原文移入 [[归档/单产品分离前/README]]；客服草案明确禁止静默上传，现行新手指引继续执行不上传用户问题、回答、文件或隐私数据的合同。

## 复检

- `node --test scripts/__tests__/memory-product-separation.test.mjs`：11/11 通过，锁定 7 项产品 Skill allowlist。
- `python3 -m unittest discover -s public/skills/tests -p 'test_*.py'`：20/20 通过。
- 旧 `jc-everything-wiki/scripts/test_everything_to_wiki.py` 已随旧 Skill 备份到工作区外；当前 Skill 仅保留按需规划 `SKILL.md`，由 Wiki Skill 合同测试和运行时 generic 测试覆盖。
- 现行范围检索不再出现 `Studio 创模式`、`Studio 文/武模式`、OpenCode `ses_*` 来源模板、MYnewapi、my-opencode 或 Tiptap 依赖合同。
- Wiki 深层巡检：现行断链 0；归档卫生记录保留，不伪造链接。
- `pnpm run test:focused`：970/970 通过；Rust `395 passed / 1 ignored`。
- `pnpm exec vue-tsc -b --pretty false`、Gateway 36/36 和 Wiki 全量 validate 通过。
- `pnpm run build:quick` 与 `pnpm run build:desktop:quick` 通过；Web/Desktop 产物审计均通过，最终 `dist/skills/` 只有 7 个产品 Skill。

## 未验证

真实 Windows、Intel Mac、iOS 升级与云绑定连续性未在本轮执行；Android 继续暂停。本轮只完成源码级 Web/Desktop 构建，没有生成或签名正式安装包；未推送、未发布。
