# 来源索引

> Wiki 保存结论与导航；原始记录保留在原位置，不复制。

## 原始记录位置

| 来源 | 位置 | 读取规则 |
|---|---|---|
| Studio 创模式 | App 会话存储或用户明确提供的导出 | 不自动生成项目内副本 |
| Studio 文/武模式 | OpenCode 项目会话 `ses_<会话ID>` | 按 `projectDir` 查询 OpenCode 会话数据库 |
| VS Code Chat | VS Code 当前项目对应工作区的 `chatSessions/<会话ID>.jsonl` | 按项目路径定位后只读 |
| 外部原文/文件 | 用户提供的原始路径 | 不复制，记录原路径 |

## Wiki 文档与原始记录对应表

| Wiki 文档 | 原始记录 | 说明 |
|---|---|---|
| [[开发/创模式原生附件直连合同SDD]] | OpenRouter `https://openrouter.ai/models`、`https://openrouter.ai/api/v1/models`（2026-07-21 只读查询）；用户确认的创模式模型优先、用户自有 Provider/K 与本地工具补位原则；AGENTS.md 产品哲学审计；Git `c48e95b1` 实施基线及 `ed752580`、`443f2d3b`、`7614ad3b`、`47c3eff4`、`344ac92d`、`d10b2d77`、`f8d21b1b`、`f6256ffe`；2026-07-21 当前用户 K 的生产 `/v1/models` 与 Chat Completions 极小 PNG/MP4/WAV 合同复测；`pnpm run test:focused`、TypeScript、Web/Desktop 构建与产物审计 | 当前 K 返回 74 个模型并包含 Gemini 3.5 Flash 与 GPT-5.6 Terra。生产合同为图片 `image_url`，视频/音频/文件 `file.file_data`；Gemini 对 378B PNG、2290B MP4、32078B WAV 和 MP4 + tools 正确识别，GPT 对同一 MP4 未读取。实现只复用当前 Provider/K，不提供公共服务；Base64 不持久化。双端 UI、刷新恢复、付费生成和三平台安装包矩阵仍未人工验收。 |
| [[巡检报告/2026-07-21-创模式核心原则一致性巡检]]、[[架构/产品架构]]、[[开发/创作模式双端统一SDD]] | 用户本轮明确确认的创模式核心原则；现行 Wiki、Git 历史与当前代码合同只读核对 | 统一“模型原生能力优先、产品补位、Skill/工具增强”的产品定义；历史实测保留，当前视频摘要与工具装配标为实现缺口，不改代码。 |
| [[开发/三项生产故障闭环SDD-2026-07-21]] | 用户提供的 Web 截图、Word 错误与 RH 任务日志；生产 API/Nginx/rh-adapter 探测；VPS `document-converter` 部署、health JSON 200、Nginx 语法检查与公网 CORS 预检；真实 DOCX 422 的完整 MarkItDown 异常；Git `b29da91b`；真实 DOCX 容器输出 5206 字节 Markdown；正式 Web 上传验收截图；`src/utils/documentMarkdown.ts`、`src/components/canvas/canvasPersistence.ts`、`src/stores/mediaTaskStore.ts`、`src/api/media-generation.ts` | 三项生产根因与本地最小修复；Word 转换已完成真实生产闭环。Web 发布、画布人工验收和 RH 真实任务回收仍未执行，不记录为完成。 |
| [[排障/ZX-Grok参考图视频真实失败交接-2026-07-21]] | 用户提供的真实控制台报错；Git `79ddbddc`、`86690b7e`、`4b17c0cf`；`src/runtime/creation/creationMediaRuntime.ts`、`src/api/media-generation.ts`、`vite.config.ts`；`pnpm run test:focused`、TypeScript、Web/Desktop 构建与产物审计 | 记录已验证的自动证据与未通的真实付费闭环；交接时先抓 proxy/WebView 的真实请求体。 |
| [[开发/韭菜盒子原生媒体编排能力SDD]] | `src/runtime/workbench/mediaReference.ts`、`mediaPlan.ts` 及测试；`src/components/chat/`、`CreationPanel.vue`、`ProjectFileTree.vue`；`pnpm run test:focused`、TypeScript、Web/Desktop 构建与产物审计；`/tmp/jc-media-orchestration-verification.txt`（`sha256:3a1e6607ddd5`） | 原生素材引用合同、动态模型目录、确认与唯一媒体执行链；真实付费、刷新恢复和三平台人工验收边界。 |
| [[排障/Web创作面板控制台红字排障-2026-07-20]] | `index.html`、`public/boot-guard.js`、`scripts/audit-web-dist.mjs`、`scripts/audit-desktop-dist.mjs`、`src/services/creationModelAvailability.ts`、`src/components/canvas/canvasDocument.ts` 及对应合同测试；服务器 `/api/runninghub/` CORS 验证回执；`/private/tmp/web-console-closeout-evidence.log`（`sha256:f24f00bc9900`） | 三类 Web 控制台错误的根因、最小修复、RunningHub 预检验证与当前全仓验证限制；原始日志只保留路径和指纹，不复制进 Wiki。 |
| [[开发/Wiki四Skill产品化升级SDD]] | `public/skills/jc-cha-wiki/`、`jc-raw-wiki/`、`jc-jian-wiki/`、`jc-xiu-wiki/`；`public/skills/tests/test_wiki_skill_migrations.py`；Raw 契约测试；`pnpm run test:focused`；`pnpm run build` | 四个 Wiki Skill 的现行范围查询、收尾证据、分层巡检、可审计修正合同与自动验证。 |
| [[学习/GitHub推送与发布边界-2026-07-20]] | 根目录 `.gitignore`、`package.json`、`.github/workflows/build.yml`；Git `main` 状态、追踪清单与对象统计；`scripts/audit-web-dist.mjs`；[[架构/产品架构]]；[[开发/v1.3.0发布失败修复SDD]] | GitHub 推送、Web/桌面发布的输入边界，以及 2026-07-20 工作区盘点。未读取会话内容。 |
| [[开发/全仓TypeScript错误修复方案-2026-07-20]] | `src/components/creation/CreationPanel.vue`；`src/components/filetree/ProjectFileTree.vue`；`src/runtime/creation/creationMediaPlan.ts`；`src/runtime/creation/creationModelRegistry.ts`；`src/services/projectFileService.ts`；`src/stores/pluginStore.ts`；`pnpm exec vue-tsc -b`；16 条相关合同测试 | 16 个 TypeScript 错误的六组根因、最小类型修复与回归证据。 |
| [[开发/自定义MCP添加SDD]] | Git a1c1fea3、93d8972b；src/components/mcp/McpManagerPanel.vue；MCP 面板合同测试 | 自定义 MCP 的三种传输、复用连接链路、Web/Desktop 边界和 UI 验收；真实远程服务调用待补。 |
| [[开发/对话Markdown正文紧凑化SDD]] | Git 01a1d85c；src/components/chat/MessageBubble.vue；Markdown 样式与语义合同测试 | HTML 源码换行被 pre-wrap 显示的根因、最小修复和回归证据。 |
| [[开发/开发历史]]（0719-MCP 用户连接与对话排版） | Git a1c1fea3、93d8972b、01a1d85c | 本阶段提交范围、验证和未完成的跨端/真实服务验收。 |
| 待补充 | 待补充 | 填写 `ses_*`、VS Code 路径或原始文件路径 |
