# 来源索引

> Wiki 保存结论与导航；原始记录保留在原位置，不复制。

## 原始记录位置

| 来源 | 位置 | 读取规则 |
|---|---|---|
| Studio 创模式 | App 会话存储或用户明确提供的导出 | 不自动生成项目内副本 |
| Studio 文/武模式 | OpenCode 项目会话 `ses_<会话ID>` | 按 `projectDir` 查询 OpenCode 会话数据库 |
| VS Code Chat | VS Code 当前项目对应工作区的 `chatSessions/<会话ID>.jsonl` | 按项目路径定位后只读 |
| 外部原文/文件 | 用户提供的原始路径 | 不复制，记录原路径 |

## Wiki 文档与原始记录对应表

| Wiki 文档 | 原始记录 | 说明 |
|---|---|---|
| [[开发/韭菜盒子原生媒体编排能力SDD]] | `src/runtime/workbench/mediaReference.ts`、`mediaPlan.ts` 及测试；`src/components/chat/`、`CreationPanel.vue`、`ProjectFileTree.vue`；`pnpm run test:focused`、TypeScript、Web/Desktop 构建与产物审计；`/tmp/jc-media-orchestration-verification.txt`（`sha256:3a1e6607ddd5`） | 原生素材引用合同、动态模型目录、确认与唯一媒体执行链；真实付费、刷新恢复和三平台人工验收边界。 |
| [[排障/Web创作面板控制台红字排障-2026-07-20]] | `index.html`、`public/boot-guard.js`、`scripts/audit-web-dist.mjs`、`scripts/audit-desktop-dist.mjs`、`src/services/creationModelAvailability.ts`、`src/components/canvas/canvasDocument.ts` 及对应合同测试；服务器 `/api/runninghub/` CORS 验证回执；`/private/tmp/web-console-closeout-evidence.log`（`sha256:f24f00bc9900`） | 三类 Web 控制台错误的根因、最小修复、RunningHub 预检验证与当前全仓验证限制；原始日志只保留路径和指纹，不复制进 Wiki。 |
| [[开发/全仓TypeScript错误修复方案-2026-07-20]] | `src/components/creation/CreationPanel.vue`；`src/components/filetree/ProjectFileTree.vue`；`src/runtime/creation/creationMediaPlan.ts`；`src/runtime/creation/creationModelRegistry.ts`；`src/services/projectFileService.ts`；`src/stores/pluginStore.ts`；`pnpm exec vue-tsc -b`；16 条相关合同测试 | 16 个 TypeScript 错误的六组根因、最小类型修复与回归证据。 |
| [[开发/自定义MCP添加SDD]] | Git a1c1fea3、93d8972b；src/components/mcp/McpManagerPanel.vue；MCP 面板合同测试 | 自定义 MCP 的三种传输、复用连接链路、Web/Desktop 边界和 UI 验收；真实远程服务调用待补。 |
| [[开发/对话Markdown正文紧凑化SDD]] | Git 01a1d85c；src/components/chat/MessageBubble.vue；Markdown 样式与语义合同测试 | HTML 源码换行被 pre-wrap 显示的根因、最小修复和回归证据。 |
| [[开发/开发历史]]（0719-MCP 用户连接与对话排版） | Git a1c1fea3、93d8972b、01a1d85c | 本阶段提交范围、验证和未完成的跨端/真实服务验收。 |
| 待补充 | 待补充 | 填写 `ses_*`、VS Code 路径或原始文件路径 |
